# Release-quality review log

All findings use visible command, browser, HTTP, or filesystem output as evidence.
Each cycle must run identical inputs before and after its correction, then the complete
test suite. North Star scores refer to `NORTH_STAR_RUBRIC.md`.

Final v17 sign-off (2026-09-06): the exact archive `74878eb46a79dd41bbae470662c974270e0a7032a5df54a022c15db36ca4b444` passes both independently restarted complete cold cycles. Each passes all 22 installed audits, the full installed `R CMD check`/browser suite (4,116 passes; zero failures/errors/warnings/skips), source/driver/archive identity and strict final resources with all 68 observed ports closed. Forty remaining dispositions are reconciled below; **all 153 finding rows are closed at their stated scope, with no unresolved Critical or High finding**. See [current independent integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md). Historical original conditions and failed attempts remain retained; B150–153 explicitly preserve uncertainty about the exact original events.

## Baseline findings

| ID | Severity | Subsystem | Visible evidence | Likely root cause | Actionable correction | Status |
|---|---|---|---|---|---|---|
| B-01 | Critical | Worker/session | Full and focused tests time out in cache startup, descendants, recovery, callback, and progress flows on R 4.6.1. | Worker event-loop/lifecycle behavior is not deterministic on the current runtime. | Trace public state transitions and pipe callbacks; repair lifecycle invariant and add defect-falsifying regressions. | Closed in cycle 1; serial suite and installed browser suite green |
| B-02 | High | Browser/app mode | Full browser run omitted expected `hello log`; isolated browser file passed. | Shared state, timing, or server lifecycle leaks across tests. | Reproduce under identical sequence, isolate process state, and assert visible output readiness. | Closed in cycle 1; installed app-view evidence and suite green |
| B-03 | High | Interaction | DOM contains no `#stop` element while client binds it; users cannot activate the interrupt route. | Frontend control omitted from HTML. | Add an accessible state-aware Stop button and browser cancellation/recovery coverage. | Closed in cycle 1; visible Stop/recovery evidence green |
| B-04 | High | Formatting | Formatting test returns `unused argument (quiet = TRUE)` with styler 1.11.0. | Alder calls an obsolete styler API. | Use supported API with surfaced output/error behavior; add compatibility regression. | Closed in cycle 1; supported styler path and tests green |
| B-05 | Medium | SQL tests | Missing-duckdb expectation changes after the namespace was loaded. | Optional dependency isolation relies only on library paths. | Test dependency resolution in a clean subprocess or inject a deterministic availability boundary. | Closed in cycle 1; isolated subprocess regression green; SQL surface later removed |
| B-06 | Medium | Quality tooling | `lintr` reports 29 findings and `.lintr` emits two configuration warnings. | Stale linter configuration and accumulated cleanup. | Repair configuration, fix actionable lints, document any narrow exclusions. | Closed in cycle 1; no lints or configuration warnings |
| B-07 | High | Process lifecycle | Environment audit counted 1,159 zombie descendants: 443 Chrome, 165 R, and related helpers; container PID 1 is `sleep infinity`. | The container does not reap orphans, while worker/browser teardown does not consistently wait for complete child shutdown. | Restart the dev container, run commands beneath `tini -s`, make Alder teardown wait/reap, and compare process counts before/after repeated flows. | Closed in cycle 1 for Alder teardown; the historical sleep-infinity PID-1 reaping limitation remains documented. The required two independent final clean restarts are now fulfilled by cold-v17-1 and cold-v17-2: each actual terminal has strict resource exit0, zero workloads/zombies/new processes/listeners and all68 observed ports closed. See [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md). |
| B-08 | Critical | Cache/reactivity | `probe-reactive-cache.R` visibly produced `[1] 20` after the upstream multiplier changed 2→3; expected 30. | Cache identity omitted captured free-variable values. | Fingerprint safe dependencies and bypass unsafe reference-like state; retain exact reactive regression. | Closed in cycle 1; identical probe returns 30 and full suite green |
| B-09 | Critical | R analysis breadth | Public probes rejected `eval(quote(1+1))` and literal `source("helper.R", local=TRUE)` before execution as `dynamic-dependency`. | Dynamic-operation blocklist did not distinguish statically visible cases. | Analyze literal eval; permit literal source as an opaque barrier with project-relative execution. | Closed — focused semantics probes and the full Cycle-2 package suite pass |
| B-10 | High | Worker recovery | Killing an idle worker left cells `done`, outputs current-looking, and no recovery control. | Worker death was detected only for an active request. | Detect failure during state reads, stale all outputs, clear values, and add Restart R with replay. | Closed in cycle 1; installed browser restart/replay evidence green |
| B-11 | High | Browser cell movement | After Move Up, Move Down left the DOM/server order unchanged under identical input. | Movement used `Map` insertion order rather than rendered/server order. | Derive movement from the current state cell order and assert round-trip. | Closed in cycle 1; identical browser reorder flow green |
| B-12 | High | Dark theme/accessibility | Visible dark-theme contrast ratios were 1.0518:1 (`.value-text`), 1.0046:1 (table header), and 1.0356:1 (cell header); screenshot `frontend-cycle-1/12-dark-contrast-postfix.png`. | Light hard-coded surfaces survived under dark foreground colors. | Replace surfaces/foregrounds with semantic theme variables and assert WCAG AA ratios. | Closed in cycle 1; screenshot and measured WCAG-AA ratios green |
| B-13 | High | Error/status UI | After Stop then successful `[1] 7`, a visible global `Interrupted` banner remained (`frontend-cycle-1/09-interrupt-recovered-postfix.png`). | Client action errors were not synchronized back to cleared server state. | Separate transport/local errors and clear server action state on accepted state. | Closed in cycle 1; identical recovery clears banner |
| B-14 | High | Startup | 2 of 8 identical launches ended `worker failed to start` after a fixed 15-second repeated-ping loop, without stderr. | Readiness polling created multiple identities and discarded exit diagnostics. | One 45-second bounded handshake with exit status and bounded stderr. | Closed — focused startup tests and complete installed Cycle-2 suite pass |
| B-15 | High | Package API | While `/api/packages` installed a missing package, `/api/state` timed out after 2 seconds (HTTP 000); install route took about 17 seconds. | Synchronous package install ran on the HTTP event loop. | Spawn/poll a cancellable child job and return 202 immediately. | Closed — focused nonblocking server evidence and warning-free full package suite pass |
| B-16 | High | Reproducibility | Release review found declarations only: no real lockfile snapshot/restore and a nonstandard pseudo-renv library. | Package metadata and sandbox paths were not integrated with renv. | Export standard renv snapshot/status/restore and use standard project library paths. | Closed — real snapshot/status/restore, sandbox tests, and complete package suite pass |
| B-17 | High | Error fidelity | Nested `stop()` visibly exposed only a message, with no condition class, call, or traceback. | Worker flattened conditions to a string. | Transport bounded structured conditions and render collapsible details. | Closed in cycle 1; structured browser/source errors green |
| B-18 | Medium | Formula analysis | Ordinary `~`/`lm(..., data=...)` probes emitted four ambiguous-data-mask warnings. | Formula syntax reused generic data-mask warning logic. | Quietly retain standalone formula globals; treat literal formula columns with explicit data as columns. | Closed — focused formula probes and warning-free full package suite pass |
| B-19 | Medium | Table settings | Settings displayed page size 10 while the visible table remained rows 1..25 (`frontend-cycle-1/14-settings-all-postfix.png`). | Existing table handles were not repaginated on config change. | Repaginate every visible table after applying page-size config. | Closed in cycle 1; five identical browser iterations green |
| B-20 | High | Delivery | Clean-install instructions omitted imported packages; only 3 of 13 exports were introduced; no CI existed. | Documentation and release automation trailed implementation. | Generate complete installation/API/workflow docs and CI from package metadata. | Closed locally — documentation, exported API, workflow validation, source manifest, and full R 4.6.1 package check pass |

## Review cycles

Cycle 1 is closed: observed before/after artifacts, the installed-package Chrome
suite, complete serial test suite (zero failures/errors/warnings/skips), lint, source
build, and R 4.6.1 `R CMD check --as-cran --no-manual` all passed. Cycle 2 is
closed: all independent visible reviews, identical-input corrections, browser and
CLI flows, deterministic bundle/lint gate, source manifest, full installed suite
(1,638 passes; zero failures/warnings/skips), process audit, and final R 4.6.1
package check passed. Evidence is under `cycle-2-integrated/`, with the final
artifact/check in `package-check-cycle2-final/`.

Cycle 3 is closed: independent scientific/runtime, browser/accessibility,
security, and release reviews captured visible defects B-70 and B-76 through
B-82; every correction has identical-input after-evidence. The authoritative
combined-source static gate passes, the source suite passes 289 tests and 1,792
expectations, the installed suite passes 1,793 expectations with zero
failures/warnings/skips, and R 4.6.1 `R CMD check --as-cran --no-manual` reports
`Status: OK`. Artifact SHA-256 is
`2d98cc9a7f806d4e078b946fc65bde2ee3a9fa7146e453f035f510b3f205c90c`;
post-check live-process count is zero and the supervised check added no zombies.
Authoritative paths are indexed in
`cycle-3/integrated/CYCLE-3-SUMMARY.md`.

Cycle 4 is closed: all focused corrections and meaningful negative controls are retained; the complete current-product source/browser gate passes4,110 expectations/403 tests and the corrected complete MCP source file passes830/40, both zero other outcomes. Exact v17 archive `74878eb46a79dd41bbae470662c974270e0a7032a5df54a022c15db36ca4b444` passes two independently restarted complete installed/browser cold cycles, each all22 installed audits, Status: OK with4,116 assertions/zero other outcomes and strict final resources/all68 observed ports closed. Both sets of25 new actual images and252-file inventories are independently reviewed. All26 active flows and the40 final dispositions are reconciled, with no unresolved Critical/High. Current evidence is indexed in [the cycle-4 summary](evidence/release-completion/CYCLE-4-SUMMARY.md) and [independent final review](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md); historical failed attempts and uncertain original-event attribution remain preserved.

## Product-direction update after the cycle-1 baseline

The complete serial suite passed with 0 failures, errors, warnings, or skips before
scope changed. ADR 0019 records the user's subsequent direction: remove dedicated
SQL cells, add a lifecycle-owning one-command CLI, make R completion/signature help
settings-controlled, and verify distinct knitr/Quarto and Pandoc publishing paths.
These enter cycle 2 after cycle 1 build/check and visible-output evidence close.

## Cycle-1 integration findings discovered during correction verification

These findings were captured from the installed application or the user's live
interaction notes before their corrections. A later green test alone does not close
them; each must have a repeated visible probe under the same input.

| ID | Severity | Subsystem | Visible evidence | Likely root cause | Actionable correction | Status |
|---|---|---|---|---|---|---|
| B-21 | High | Editor layout | `frontend-cycle-1/15-current-editor-geometry.png` and `probe-editor-geometry.R` measured gutter line 1 at y=141.5 but source line 1 at y=222.7; `.cm-scroller` was `display: block`. | A CSS override broke CodeMirror's gutter/content flex geometry. | Restore CodeMirror scroller layout, assert every gutter/source-line top aligns at multiple widths, and recapture the identical probe. | Closed — installed attempt-11 geometry/screenshots show aligned gutters and intended 920px notebook width; browser suite passes |
| B-22 | Medium | Navigation/accessibility | The same DOM probe returned a null cell accessible label and 16-pixel-wide edge minimap targets; the user reported missing cell numbers and a broken right-side selector. | Cell identity was implicit and the minimap was styled as a tiny edge strip without usable hit targets. | Render stable visible/accessible Cell N or name labels and an inset >=24px numbered navigator with focus/click coverage. | Closed — attempt-11 visible/AX evidence and trusted keyboard navigation pass in the full browser suite |
| B-23 | Medium | Cell organization | `dev/inbox/bugs-issues.md` records that cells cannot be click-dragged into a new order. | Ordering was exposed only through menu commands. | Add an explicit keyboard-accessible drag handle, server-backed reorder, and pointer/keyboard regression evidence. | Closed — pointer drag plus labelled keyboard Up/Down reorder pass against server-backed ordering |
| B-24 | High | R-first product scope | The installed Add Cell UI exposed SQL, and source/API/search evidence showed a dedicated SQL type, helper, DuckDB fallback, docs, and tests; the user explicitly identified this as out of scope. | An inherited parity checklist overrode the R-focused North Star. | Remove dedicated SQL chunks/helper/dependency/API while preserving legacy bytes as ordinary R and allowing unrestricted DBI/dbplyr code. | Closed — dedicated SQL surface/dependency/API is absent; legacy delimiters round-trip as R and ordinary DBI code is covered by the complete suite |
| B-25 | High | Launch/lifecycle | `dev/inbox/bugs-issues.md` records unclear start/open/stop behavior and ports requiring manual process kills; README taught an interactive `start_alder()` server workflow. | No lifecycle-owning command or client-initiated graceful shutdown existed. | Ship one `alder notebook.R` command with browser ownership, signal cleanup, authenticated Shutdown, idle timeout, status, and orphan checks. | Closed locally — installed one-command lifecycle, authenticated shutdown, idle safety, signals, exit codes, and zero-process audit pass |
| B-26 | High | R editor assistance | The installed editor did not offer regular tab completion or argument/signature help controls requested for an RStudio-like workflow. | LSP/editor support was partial and not wired to persisted, independent settings. | Add R completion and signature help plus persisted toggles and visible browser regressions for enabled/disabled behavior. | Closed — real R completion/signature/browser toggle/retry flows and full installed suite pass |
| B-27 | Medium | Live diagnostics | `dev/inbox/bugs-issues.md` records transient syntax errors appearing continuously while source is still being typed. | Validation debounce was shorter than a normal editing pause and could not be disabled. | Use at least a 1000ms quiet period, make live diagnostics toggleable, and test that intermediate syntax is not reported early. | Closed — delayed/toggleable diagnostics, default-off lintr, retained Alder diagnostics, and installed browser tests pass |
| B-28 | Critical | Run/widgets | The user observed a slider snapping back with `widget value rejected`, a newly added exact `summary(peng$Sepal.Length)` cell doing nothing, `Selection deleted`, and intermittent Run behavior. Earlier Session/Chrome replay produced slider value 5/downstream 128 and repeated summary output. The Cycle-3 full shape-fixed scientific replay again reached 5/128, but its prescribed repeat reached 4 and then lost the next trusted Right. Focused before-evidence records continuously changing output/widget node identities, `BODY` as the active element, and zero trusted widget events without a visible error. | `renderOutputs()` destroyed all output records on every state poll; the first keyed repair still re-appended connected records, which Chrome treats as a focus-losing DOM move. Pending updates also disabled continuous native controls despite an existing latest-value coalescer. | Reconcile keyed output records without moving connected nodes, patch widget subtrees in place, keep continuous controls usable while coalescing, lock only one-shot actions, and replay the exact trusted 3→5 then 5→4→5 and summary flows. | Closed by independent identical visible replay: node identity and focus survive polls, trusted rapid 3→5 and 5→4→5 reach 128, 150, and 128 without blur, a new summary runs/reruns, 96/96 plot fetches return 200, and all browser/runtime/error channels plus lifecycle checks are clean. The focused installed regression is also green. |
| B-29 | Critical | Stop/worker | With `Sys.sleep(3); 'completed-not-stopped'`, immediate Stop returned a run id but the cell finished `done` after 4.36s; acknowledged Stop completed in 0.219s and browser Stop took 5.335s. | `Worker$interrupt()` signalled only after `executing_req` was set, dropping a cancellation requested before the worker start acknowledgement. | Queue one request-scoped pre-ack interrupt, signal exactly once on matching acknowledgement, clear it on every terminal path, and prove <1.5s stop/recovery. | Closed — identical pre/post-ack probes, recovery tests, real Chrome Stop, and complete installed suite pass |
| B-30 | Medium | Dataflow visualization | `dev/inbox/bugs-issues.md` records an unusable horizontal layout and an unattractive vertical layout in the installed graph view. | Layout heuristics and viewport treatment do not scale to realistic notebook DAGs. | Capture representative DAG screenshots, improve layout/fit/navigation, then repeat the same graphs at desktop/mobile widths. | Closed — attempt-11 installed desktop/mobile screenshots and geometry show readable 190x46 nodes, directions, scrolling, controls, and zero overlap |
| B-31 | High | Client error observability | The user encountered at least two visible click errors while the review logs claimed no errors. | Browser review collected server state but did not comprehensively capture action failures, console exceptions, and rejected network requests. | Add a bounded visible client event/error log and review probes that reconcile UI banners, console output, HTTP failures, and server state. | Closed — bounded client log plus console/runtime/security/network/server reconciliation is clean in attempt 11 and the full browser suite |
| B-32 | High | Publishing | The prior generic export surface did not prove a knitr-backed Quarto path distinct from direct Pandoc, as explicitly requested. | Engine semantics and provenance were implicit. | Provide explicit `alder_render(engine=)` contracts, preserve conditions, stamp provenance, and inspect both real engine outputs. | Closed — both real engines, visible outputs/provenance, failure conditions, CLI render paths, and full package suite pass |
| B-33 | High | Base graphics/runtime | The isolated Cycle-2 publishing probe `publishing-cycle-2/01b-layered-base-plot-observed-failure.txt` ran a single ordinary R cell containing `plot(...); abline(...)`; Quarto completed, while Alder's direct-Pandoc execution failed with `plot.new has not been called yet`. The unchanged failing notebook is `publishing-cycle-2/scientific-layered-base-plot.R` (SHA-256 `177321112dc8498eff501fbb02b86138b7c357644a58db02f5063ca939ef54f9`). Post-fix `publishing-cycle-2-after/worker-graphics-layered-plot.png` visibly contains the blue series and dashed red overlay in one 800x500 image. | The worker captured a base plot after every top-level expression by closing the cell graphics device, then opened a blank replacement before the next low-level graphics call. | Keep one graphics device alive for the complete cell, coalesce low-level additions into the current plot, preserve separate high-level plots, and replay the identical notebook through editor/headless/Pandoc paths. | Closed — unchanged installed fixture exits 0; layered, independent-page, `mfrow`, renderer-cause, and browser regressions pass |
| B-34 | High | CI/release gates | `cycle-2/ci-gate-audit.txt` observed matrix values `4.6.1,devel` but both lint and editor-bundle steps used `matrix.r == 'release'`, which evaluated `reachable=FALSE`. | The runtime matrix was pinned without updating an older symbolic release guard. | Guard those steps with the actual primary runtime (`matrix.r == '4.6.1'`) and validate the workflow plus both commands locally. | Closed locally — workflow guards validate and the same bundle/lint/full package gates pass on R 4.6.1 |
| B-35 | High | Worker environment | `runtime-observability-cycle-2/10-cli-start.png` visibly lists `project_lib` and `sandbox_lib` beside the notebook's sole `x` variable. Post-fix worker state exposes neither binding unless notebook code deliberately defines it. | Worker bootstrap library variables were assigned in the notebook global environment. | Move bootstrap state into a private closure and regress both variable inspection and valid user bindings with those names. | Closed — private bootstrap plus real-worker absence/name-collision regressions pass |
| B-36 | High | Pandoc/Markdown publishing | `publishing-cycle-2/pandoc-full-page.png` and `pandoc-inspection.json` show each Markdown cell twice, literal comment markers, and nine headings versus Quarto's three. Post-fix `publishing-cycle-2-after/success-pandoc-dom.txt` and `scientific-pandoc.png` show three headings and one occurrence of every narrative marker. | Direct-Pandoc and generic Markdown serializers emitted both canonical Markdown source and its duplicate runtime-render output. | Emit Markdown source exactly once and assert heading/text counts under the identical fixture. | Closed — identical installed-output replay and focused publishing tests pass |
| B-37 | High | Quarto condition fidelity | `publishing-cycle-2/03-failure-cli.txt` shows a fatal Quarto cell reporting its error while suppressing the preceding same-cell message and warning. Post-fix `publishing-cycle-2-after/failure-quarto-cli.stderr` and `failure-quarto-verification.txt` show message, warning, and fatal error once each in order, exit 1, and preserved output. | Quarto's failed knit discards intermediate message/warning output; Alder also initially duplicated failed subprocess output between relay and the raised condition. | Tee only otherwise-lost message/warning conditions, retain Quarto's native fatal report, and carry failed-engine diagnostics once in the raised condition. | Closed — exact marker counts `1,1,1`; installed identical-input replay and focused tests pass |
| B-38 | Critical | Idle lifecycle/data safety | `runtime-observability-cycle-2/43-idle-unsaved-result.json` records `changed:true`, an acknowledged body `x <- 999`, CLI exit 0, closed port, and unchanged disk source `x <- 1`. | Idle teardown ignored dirty Session state and never invoked conflict-checked save. | Atomically save acknowledged changes before idle stop; on conflict/failure retain the live process and expose `idle_save_failed`. | Closed — installed CLI autosave/conflict identical flows, widened deterministic check replay, full suite, and zero-process audit pass |
| B-39 | High | R editor assistance | `runtime-observability-cycle-2/06-direct-lsp-start.txt` records default 5-second LSP initialization failing after 6.286s while 15 seconds succeeds in 5.448s; browser completion/signature requests return 503 with no visible status. | The cold-start deadline was too short, startup errors were discarded, and polling stopped between requests. | Use a realistic bounded cold-start, continuous diagnostics/death polling, bounded stderr/exit details, persistent visible service state, and retry. | Closed — real cold start/completion/signature/death/retry browser evidence and complete installed suite pass |
| B-40 | High | Code editor | `runtime-observability-cycle-2/23-browser-exceptions.json` captures CodeMirror `RangeError: Field is not present in this state` during assistance reconfiguration. | Alder removed the autocomplete StateField while an active blur/tooltip handler could still consult it; reviewer and prior test also removed tooltip DOM out-of-band. | Keep the completion extension/state field stable, gate its source and close through CodeMirror APIs, remove invalid DOM mutation, and fail on browser exceptions. | Closed — lifetime-stable completion state, supported close boundary, deterministic bundle, and real-browser exception gate pass |
| B-41 | High | Dataflow correctness | `graph-a11y-cycle-2/review-summary.json` shows singleton adjacency serialized as strings: `report_table` disappears from descendants and dependent cells rank at zero. | JSON shape varied with vector length and the client ignored non-array adjacency. | Normalize adjacency to arrays at the API boundary and assert descendant/rank behavior. | Closed — attempt 11 has array-valued 0/1/multi adjacency for all 27 nodes, exact 34-edge paths/ranks, plus focused R/browser regressions |
| B-42 | High | Graph/navigation | Cycle-2 graph screenshots measure nodes around 25–32 by 8–9px with 3–4px labels in every orientation/viewport; the navigator overlaps the notebook by 30px. | Fit-to-panel SVG scaling shrank natural graph geometry and the panel did not reserve a layout track. | Preserve readable intrinsic dimensions, scrolling/zoom/fit/expand, direction arrows, and non-overlapping tracks. | Closed — root inspected attempt 11: 190×46 minimum nodes, readable labels, arrowheads, scrolling and controls, with zero desktop overlap |
| B-43 | High | Mobile panels | `graph-a11y-cycle-2` geometry records Variables at 1563px and Outline at 1218px in a 390×844 viewport, placing final rows off-screen. | Mobile drawer content grew the page instead of using a viewport-bounded flex/scroller. | Bound drawer height to the viewport and make panel content internally scrollable; repeat exact mobile flows. | Closed — 390×844 attempt-11 drawer is viewport-bounded, internally scrollable to final Variables/Outline rows, and closes to a zero rectangle |
| B-44 | Medium | Outline/drag accessibility | Cycle-2 AX/interaction evidence exposes narrative paragraphs as outline headings and a focusable drag button for which Enter has no behavior. | Markdown comment stripping treated ordinary text as headings, and pointer drag semantics were assigned misleading button semantics. | Parse only actual Markdown heading markers; provide keyboard reorder semantics/menu or remove button role. | Closed — outline contains only five real headings; pointer grip is aria-hidden/Tab-skipped and labelled Up/Down buttons pass trusted keyboard mutation 36/36 |
| B-45 | High | Browser security observability | `graph-a11y-cycle-2/browser-log-entries.json` contains 106 CSP `style-src` errors from ordinary CodeMirror use while console/runtime/network/server channels all report zero. | CodeMirror-generated CSS and required runtime geometry styles were blocked by a CSP that lacked nonce and style-attribute provisions. | Supply a per-server CodeMirror CSP nonce, narrowly allow style attributes, retain strict scripts, and fail browser gates on security-log errors. | Closed — attempt 11 records zero Browser Log entries/warnings/errors, console/runtime/window errors, or failed/non-2xx network requests |
| B-46 | High | Desktop editor layout | Root inspection of the first distinct after screenshot `graph-a11y-cycle-2-after/01-desktop-editor-navigation.png` and `geometry-desktop.json` shows a closed drawer shrinking the notebook to 562.375px and leaving 534.125px blank; the supposedly hidden panel still has a 325.625x819px layout rectangle at the page bottom. | The author rule `#dataflow-panel { display: flex; }` overrides the browser's `[hidden]` rule, so the hidden drawer remains a third implicit grid item even under the two-column `panel-closed` layout. | Explicitly make `#dataflow-panel[hidden]` non-rendering, assert it has no layout rectangle, and require the closed desktop notebook to use its intended near-920px column before repeating the preserved after probe. | Closed — attempt 11 shows display:none/zero rectangle and a 920px closed-panel notebook; failed attempts 1–10 remain preserved |
| B-47 | High | Layout condition fidelity | `condition-fidelity-cycle-2/02-public-http-probe.log`, `02d-layout-fixture.txt`, and initial state show a malformed retained sidecar becoming `layout:null`; `GET /api/layout` returned 200 and no state/stderr diagnostic existed. | `Session$initialize()` caught every `alder_layout_read()` error and returned `NULL`. | Preserve an `invalid_layout` state/global diagnostic, log it once, make the layout GET distinguish invalid from absent, retain bytes until an explicit valid write, and replay the identical installed fixture. | Closed — identical installed replay retains SHA `3b3b6614...89b`, logs once, exposes persistent `invalid_layout`, returns HTTP 400, and cleanly shuts down |
| B-48 | High | Gallery condition fidelity | `condition-fidelity-cycle-2/02b-gallery-index.html` omitted malformed Alder `bad.R`; `/n/bad.R` returned generic 404 and stderr was empty although direct parsing reported `malformed YAML metadata`. | Gallery catalog discovery converted every parser error to `NULL`, conflating a broken Alder file with an intentional delimiter-free R file. | Render an accessible unavailable card with bounded cause, log once, return specific 400 `invalid_notebook`, retain ordinary plain-R omission, and replay the identical gallery. | Closed — identical installed SHA `83906025...f39e` renders one role-alert card, logs once, returns 400 `invalid_notebook`, omits plain R, runs the valid notebook, and closes cleanly |
| B-49 | High | Editor diagnostic policy | The user observed apparent continuous lintr notes. Inspection of the installed languageserver 0.3.18 runtime showed default `diagnostics=TRUE`; `text_document_did_change()` requests `run_lintr=TRUE`, and `diagnose_file()` calls `lintr::lint()` after each edit. Alder defaulted `editor.live_diagnostics=TRUE`. | Alder treated the language server's style-lint stream as mandatory editor diagnostics instead of separating it from R completion and Alder safety checks. | Disable the server-side lintr task by default, make the existing setting explicit opt-in, always retain Alder parse/safety diagnostics, prove completion/signature independence, and capture installed browser/process evidence. | Closed — independent installed R 4.6.1/Chrome before-and-after evidence proves default-off, opt-in, clear-on-disable, completion/signature independence, retained Alder diagnostics, and clean browser/stderr channels |
| B-50 | High | Windows delivery | `release-surface-cycle-2/source-package-files.txt` contains only POSIX `inst/exec/alder`; the installed Rd explicitly says Unix and `alder_install_cli()` rejects non-Unix although DESCRIPTION has no OS restriction. | The lifecycle-owning launcher and installer were implemented only as a POSIX shell path. | Package a Windows command launcher over the same `alder_cli()` API, install it atomically by platform, document PATH/setup, and add Windows R 4.6.1 CI plus help/version/launch/cleanup coverage. | Closed locally to the available platform — both launchers are packaged, selection/bytes are verified, and Windows R 4.6.1 CI runs real launch/authenticated shutdown/port/descendant cleanup; native CI remains an external gate |
| B-51 | Medium | Installation ergonomics | The exact documented clean Pak install in `release-surface-cycle-2/raw-transcript.txt` succeeded warning-free but installed Alder plus 50 dependencies and took 15m56s; stringi/httpuv/later source compilation dominated. | Required `languageserver` brings its R-language tooling stack; the isolated Linux environment lacked binary packages. | Retain required completion/signature help, document binary-repository/source-build expectations, record clean-install timing in CI, and avoid pretending this dependency cost is optional. | Closed as an explicit dependency tradeoff — README documents measured source cost and recommends trusted binaries without making required editor assistance optional |
| B-52 | Low | New-notebook CLI UX | The installed `alder missing.R --no-open` visibly started an empty unsaved notebook while render rejected the missing input, but CLI help/README described only opening an existing notebook. | Creation-on-first-save is intentional in `start_alder()` but absent from the primary one-command documentation/status output. | Document `alder new-analysis.R`, visibly identify a new unsaved path, and regress create/save/relaunch plus never-saved behavior. | Closed — visible announcement/help/docs plus cross-platform never-saved and 11-expectation save/shutdown/relaunch exact-source regressions pass |
| B-53 | Low | Source-package hygiene | `release-surface-cycle-2/source-package-files.txt` shows internal `tests/AGENTS.md` in the 86-entry tarball while root task guidance is excluded. | `.Rbuildignore` excluded only root `AGENTS.md`. | Add a precise nested ignore, rebuild, and audit the complete manifest without deleting repository guidance. | Closed — final 86-entry source manifest contains both launchers and excludes nested guidance plus all task/state/flow files |
| B-54 | High | Dataflow condition resilience | Root inspection of `diagnostic-policy-cycle-2/artifacts/51-parse-after-disable.png` shows the expected `syntax-error` for `x <-`, but both the global banner and Dataflow panel also expose `Dataflow projection failed: 'names' attribute [5] must be the same length as the vector [4]`. | `.df_json_value()` used `out[[i]] <- NULL` for a legitimate JSON-null diagnostic field, deleting that slot before assigning the original five names to four retained values. | Make every dataflow projection total over malformed/incomplete editor source, retain the cell's actionable syntax diagnostic, remove the internal projection failure, and replay the exact installed browser input. | Closed — null-preserving assignment, complete dataflow tests, focused installed Chrome, and exact after screenshot show only the intended syntax/safety diagnostics |
| B-55 | Medium | Diagnostic severity presentation | `diagnostic-policy-cycle-2/artifacts/21-explicit-opt-in-state.json` classifies opt-in lintr notes as `info`, but the corresponding DOM/screenshot assigns `diagnostic-error` and displays them in red beside actual blocking Alder errors. | `renderDiagnostics()` mapped only `warning` specially and treated every other level as `error`. | Preserve `info`, `warning`, and `error` severity in DOM classes and styling; regress that opt-in lintr notes are informational while Alder safety errors remain errors. | Closed — exact installed after DOM uses `diagnostic-info` for both lintr notes and `diagnostic-error` for Alder safety errors; root inspected the corrected neutral/red screenshot |
| B-56 | High | Browser gate integrity | `cycle-2-integrated/08-full-suite.log` failed the newest lintr-severity and incomplete-source flows, while the same source-built installed probes were green. Hash/timestamp inspection showed the child loaded a 20:41 ambient site-library build while current `app.js` was modified at 20:53. | `start_browser_server()` launched a child without the suite-owned fresh library, splitting source-loaded parent and stale installed child. | Build once through `alder_cache_lib()`, prepend that exact library to the browser server process, and rerun the original suite without relying on ambient packages. | Closed — exact probes and unfiltered installed package suite pass from the suite-owned library |
| B-57 | Critical | Explicit shutdown/data safety | Focused Chrome inspection observed server `changed=TRUE` while the client snapshot remained `changed=FALSE` with acknowledged generations; immediate Shutdown skipped `confirm()` and could discard the edit. | Shutdown decided from stale client state without first flushing debounced edits and refreshing authoritative state. | Lock the control, flush edits, refresh state, confirm dirty discard, and restore controls on rejection before issuing authenticated shutdown. | Closed in focused replay — confirmation count 1, dirty state retained after rejection, controls restored, no client/server errors |
| B-58 | High | Packaged server lifecycle | The browser displayed “Alder shut down” and stopped httpuv, but `ctx$proc$is_alive()` remained true after ten seconds. | `inst/worker/server.R` used an unconditional `repeat later::run_now()` loop and never observed `srv$lifecycle$stopped`. | Use the same lifecycle-aware pump invariant as the one-command CLI and require process exit after authenticated shutdown. | Closed — exact fresh installed Chrome regression exits the launcher; 9/9 expectations pass |
| B-59 | Low | MCP contract test | `cycle-2-integrated/08-full-suite.log` failed only because `unlist()` retained internal class `AsIs`; direct wire output was the correct JSON array `["code","markdown"]`. | The test asserted incidental R serializer metadata instead of the public JSON-RPC representation. | Round-trip `tools/list` through `mcp_json()`/`fromJSON()` and assert the public array values without changing serializer shape. | Closed — complete focused MCP file passes 24 expectations with correct serialized wire contract |
| B-60 | Medium | CLI test diagnostics | Independent audit found `cli_expect_closed()` calling `testthat::fail()` inside an unbounded `repeat`; a real timeout could flood the reporter indefinitely. | The helper recorded failure but did not return or break. | Return immediately after the single bounded failure while retaining the final closed-port assertion on normal completion. | Closed — focused CLI plus unfiltered full package suite pass |
| B-61 | High | Browser gate environment | Corrected full suite `17-full-suite.log` and isolated `18-htmlwidget-focused-before.log` failed the htmlwidget flow; `19-htmlwidget-state.json` showed a terminal error after 1.46s: `The 'HOME' environment variable must be set before running Pandoc.` | Supplying a one-variable `env = c(R_LIBS=...)` to processx replaced the child's complete inherited environment while fixing B-56. | Preserve the complete inherited environment and override only `R_LIBS`; replay the same self-contained htmlwidget through worker, iframe sandbox, and artifact GET. | Closed — exact 4/4 replay and unfiltered installed browser suite pass without warnings or skips |
| B-62 | High | Source-package structure | First Cycle-2 `R CMD check --as-cran` emitted a package-subdirectory warning: non-empty `inst/exec` is also used by R. | Launchers were placed beneath `inst/` instead of R's canonical top-level `exec/` package directory. | Move both byte-identical platform launchers to top-level `exec/`, update repository map/manifest gates, preserve Unix mode 0755, and rebuild. | Closed — final source manifest contains canonical `exec/alder` and `exec/alder.cmd`; package check is `Status: OK` |
| B-63 | Medium | Dependency metadata | The same check emitted an unused-Import note for required `languageserver` and an undeclared test dependency warning for `ps::`. | The child-process language-server invocation was invisible to namespace checks, while the new process-cleanup regression omitted `ps` from Suggests. | Keep languageserver required in Imports and import its exported `run` symbol; declare `ps` in Suggests; rerun a clean check with forced suggests. | Closed — dependency code/test checks pass with forced Suggests and final package check is `Status: OK` |
| B-64 | High | Browser check portability | The second Cycle-2 `R CMD check` skipped all 37 browser tests because Chrome could not create its singleton socket beneath the deeply nested check `TMPDIR`; the same suite was green outside that path. | Chrome's Unix-domain singleton socket exceeded the platform path limit even though Alder itself had started correctly. | Allocate browser projects and Chrome scratch space beneath a short private `/tmp` root on Unix, retain fallback behavior elsewhere, and replay from a deliberately long `TMPDIR`. | Closed — long-path probe passes 5/5 and the unfiltered check runs the complete browser suite with zero skips |
| B-65 | High | Installed gate integrity | `package-check-final-clean/04-00check.log` reports 35 failures, 0 warnings, 0 skips, and 1,563 passes; saved reproducers clustered in browser/CLI child starts and Quarto condition relay although their exact fresh-install tests passed. | During `R CMD check`, startup code adds `alder.Rcheck` only to the parent's `.libPaths()`. `alder_cache_lib()` returned an empty override for any installed package, so vanilla children silently loaded the older site-library Alder. | Derive the library from the exact loaded namespace, prepend it to every child environment while preserving all other variables, use the platform path separator, and assert that the resolved child package path equals the loaded package path. | Closed — exact context probes and final unfiltered installed suite pass 1,638 expectations from the checked package |
| B-66 | Low | Failure evidence | The failed clean check preserved `00check.log` but not the full test transcript because R renamed it `testthat.Rout.fail`, while the harness copied only `testthat.Rout`. | The evidence collector assumed the success filename on every exit path. | Copy both `testthat.Rout` and `testthat.Rout.fail` before deleting the private check root; validate the script with Bash and ShellCheck. | Closed — `package-check-final-pinned/05-testthat.Rout.fail` and the later diagnostic transcripts were retained before cleanup; Bash and ShellCheck are clean |
| B-67 | Medium | CLI check portability | `package-check-final-pinned/05-testthat.Rout.fail` records 16 CLI failures whose exact output is `'Rscript' should not be used without a path -- see par. 1.6 of the manual`; every browser child and direct absolute-R probe passed. | `R CMD check` intentionally prepends a failing `Rscript` shim in `R_check_bin`, while the installed user launcher correctly resolves `Rscript` from an ordinary user's PATH. The integration helper forwarded the artificial PATH unchanged. | In CLI integration tests only, prepend the actual `R.home('bin')` and remove the relative check-only `R_TESTS`; assert both invariants while preserving every other user-like environment variable. | Closed in exact check replay — `package-check-cli-publish-final/03-R-CMD-check.log` is `Status: OK`, with real CLI starts, shutdown, idle safety, and signals |
| B-68 | High | Quarto error fidelity | The filtered check transcript `package-check-publish-diagnostic/05-testthat.Rout.fail` shows Quarto reporting `Error:\n! UF22 INTENTIONAL FATAL ERROR` and then a simple tilde-delimited `<error/rlang_error>` block repeating the identical signature; marker counts were `1,1,2`. | The check reporter enables rlang condition printing in the nested render, so Quarto emits both its ordinary fatal line and a redundant simple condition block. | Remove only a simple rlang error block whose exact error signature already appeared; retain blocks with any unique text/backtrace, all warnings/messages, ordinary repetition, and the nonzero render failure. | Closed in exact check replay — focused publishing is green and `package-check-cli-publish-final` finishes `Status: OK`; preservation regressions cover distinct blocks and ordinary repeated text |
| B-69 | Medium | CLI idle-test determinism | The first corrected CLI/publishing check failed only at the two edit POSTs with `curl_error_couldnt_connect`: an independently scheduled server exhausted a synthetic 0.8-second idle window while the check parent was descheduled. No edit was acknowledged, so autosave/conflict behavior had not yet run. | The test deadline was shorter than R-CMD-check scheduling jitter and conflated process scheduling with the intended post-edit safety contract. | Give the test client five seconds between its presence poll and edit, then keep the same assertions for idle exit/save, conflict-blocked liveness, visible `idle_save_failed`, and byte-identical external content. | Closed — identical focused flows pass 4/4 and 6/6 in `41-idle-window-focused.log`; exact CLI/publishing package check is `Status: OK` |
| B-70 | Critical | Widget protocol/runtime | Cycle-3 `scientific-runtime/10-state-initial.json` and the visible `10-initial-notebook.png` show the supported widget gallery ending in `invalid worker response`, killing the worker and marking every earlier scientific output stale. The exact isolation matrix fails its composite cases. The first corrected browser replay then exposed singleton `indices` serialized as a scalar and crashed `.map()`. Finally, independent current-source `http-results-before-request-fix.json` (40/44) and `mcp-results-before-request-fix.json` (27/31) show valid standalone/nested range arrays rejected while named JSON objects for multiselect/table are accepted and mutate state. | JSON arrays arrive as unnamed lists at worker, Session-response, and public Session-request boundaries while validators assumed atomic vectors or indiscriminately `unlist()`ed objects; `auto_unbox = TRUE` also collapsed singleton multiselect/table arrays without an `AsIs` marker. | Apply one strict schema-declared unnamed-array decoder at all three boundaries, recursively normalize nested specs, preserve empty/single/multi response arrays, reject named objects without mutation, and replay Session, HTTP, MCP, and visible scientific inputs identically. | Protocol repair closed by identical-input evidence: focused Session 22/22, HTTP 12/12, and MCP 5/5 pass; six-case isolation is 6/6; independent fresh-install Session/HTTP/MCP replays are 46/46, 52/52, and 39/39 with exact committed values/array shapes, named-object and wrong-shape rejection, valid recovery, visible dependent conditions, clean stderr, and a live worker. Final integrated scientific completion and Cycle-3 suite remain cycle gates, not unresolved protocol behavior. |
| B-71 | Low | Review infrastructure | The first Cycle-3 browser and scientific launches fail before app inspection with Chrome `SingletonSocket` path errors beneath deeply nested evidence directories. | Standalone review probes did not reuse the short private Chrome scratch-root invariant already established by B-64. | Preserve the invalid attempts, use a short `/tmp` Chrome root in all review probes, and require the corrected run before classifying product behavior. | Closed as a review-harness defect; short-root browser/runtime evidence passes 30/30 and the scientific reviewer preserved both attempts. |
| B-72 | Low | Review infrastructure | `release-publishing/06-publishing-source-probe.log` reports Quarto unable to run because `HOME` is unset while direct Pandoc succeeds. | The review helper supplied only `R_LIBS` variables to `processx`, replacing rather than extending the inherited environment—the same harness pattern identified by B-61. | Preserve the invalid run, inherit the complete environment, override only the two library variables, and replay byte-identical publishing inputs. | Closed as a review-harness defect; corrected log `07-publishing-source-probe-corrected.log` passes 58/58, including both engines and fatal-condition fidelity. |
| B-73 | Low | Review infrastructure | The first Cycle-3 navigation replay reports a non-reacting Move Up control and focuses the drag grip even though the accepted browser regression is green. | The historical probe sent a synthetic `keyDown` carrying carriage-return text and programmatically focused a pointer-only, aria-hidden, Tab-skipped grip. | Preserve the invalid attempt, reconcile with current interaction contracts, use trusted `rawKeyDown` plus `keyUp`, and follow the actual Tab order. | Closed as a review-harness defect; the corrected trusted replay passes 36 reorder expectations, proves the grip is omitted from native Tab order, and preserves the invalid attempt separately. |
| B-74 | Low | Review infrastructure | After all 30 runtime assertions pass, `browser-accessibility/runtime/00-probe-transcript.log` emits a warning reading a vanished `/proc/<pid>/stat`. | A process can exit between `file.exists()` and `readLines()` in the evidence collector. | Treat only the expected vanished-process race as absent without warning, surface every other I/O failure, and rerun the identical probe warning-free. | Closed by identical-input replay: 30/30 assertions pass with zero R or vanished-process warnings; console, Runtime, window, security, and unexpected network channels are empty; the sole 503 and two stderr lines are the deliberately surfaced LSP-death condition; authenticated Shutdown exits 0, closes the port, and leaves zero tracked descendants. |
| B-75 | Low | Review infrastructure | The first scientific probe's post-failure lookups cascade into missing-cell and CDP argument errors even after removing the two failing widget kinds. | The evidence helper reads obsolete `cell$name`; current public state exposes the notebook name as `cell$options$name`. | Preserve the invalid attempt, correct only the evidence lookup/filter, and replay the byte-identical complete scientific notebook. | Closed as a review-harness defect. The corrected lookup completed the scientific replay; its remaining genuine product findings were isolated separately as B-78 through B-82. |
| B-76 | High | Dataflow graph | Root inspection of `browser-accessibility/navigation/corrected/05-desktop-graph-horizontal.png` and `12-mobile-graph-horizontal.png` confirms adjacent 190×46px nodes visibly obscure one another; measured geometry records 16 pairs overlapping by 72×46px at both 1350px and 390px. Vertical orientation has zero overlaps. | Horizontal rank positions advance by `nodeHeight + rankGap` even though the rank axis contains 190px-wide nodes. | Use `nodeWidth + rankGap` for horizontal rank spacing, retain `nodeHeight + rankGap` vertically, and assert pairwise non-overlap at desktop and mobile widths under the identical 27-node DAG. | Closed by exact visible replay: all 351 pairs have zero overlap and zero clipped labels at 1350×900 and 390×844, with 190×46px nodes and 262px horizontal rank steps. Root inspected both screenshots; the focused installed regressions pass 41/41 and the evidence manifest verifies. |
| B-77 | Medium | Outline accessibility | `trusted-keyboard-navigation.json` records trusted Enter `keydown`/`keyup` on the focused outline button with no click or navigation, while trusted Space and pointer activation focus `cell-3`; focus styling is visibly present. | Outline panel links rely on inconsistent browser-default button activation while the panel boundary explicitly handles other keyboard controls. | Handle Enter and Space once at the panel-link boundary, prevent only the handled default, and regress trusted keyboard activation without double navigation. | Closed by exact trusted replay: Enter and Space each reach the focused button, are default-prevented by Alder, call navigation exactly once, emit zero click events, and focus `cell-3`; visible screenshots, event telemetry, clean shutdown, and the 41/41 focused regressions agree. |
| B-78 | High | Datetime widgets/browser | The complete scientific replay rendered the supported datetime control empty and captured 119 Chrome warnings that its RFC3339 `...Z` value did not conform to `datetime-local`. | Alder passed its canonical UTC wire value directly into a zone-free HTML input instead of translating at the DOM boundary. | Keep UTC RFC3339 on the wire, add a strict UTC-wire/zone-free-DOM codec, bind UTC-labelled min/max and `step=1`, validate both Session and worker payloads, and require a non-UTC browser-zone round trip with zero warnings. | Closed — focused installed Chrome and independent scientific evidence show the expected local DOM strings, canonical UTC updates/dependent output, stable node identity, and zero warning/error channels. |
| B-79 | High | Plot artifacts/browser | Rapid reactive plot changes produced transient 404 responses for immutable `/plot/...` URLs that had already appeared in delivered state. | The next successful commit unlinked the previous artifact immediately, racing the browser fetch promised by the earlier state snapshot. | Retire superseded contained artifacts after a bounded grace period, retry only failed retirement, and retain unconditional session-root teardown. | Closed — the installed B-28 regression refetches the prior URL as a valid PNG, independent scientific replay records 96/96 rapid plot responses below 400, and shutdown leaves no descendants. |
| B-80 | High | Nested widgets/Session | A direct child update inside `dictionary -> form -> text_input` returned HTTP 500 instead of recording a draft. | Nested form discovery traversed only the current child and lost the root specification needed to resolve the form owner and commit boundary. | Traverse from the output root while retaining the addressed path/form ancestor, validate the nested operation, and regress draft-without-dependent-rerun followed by explicit submit. | Closed — Session and installed-browser regressions accept the nested draft, keep committed value `NULL` and the dependent unchanged, then commit and rerun only on Submit. |
| B-81 | High | Form interaction/browser | In the exact rapid flow, typing a second nested-form value and immediately pressing Submit emitted overlapping draft and submit operations; the latter returned intentional HTTP 409 `operation_in_progress` and the visible action failed. | Frontend operation keys included widget kind, while Session serializes one operation per `{name,path}`; form submit therefore did not wait for in-flight child drafts beneath its subtree. | Flush all pending descendant drafts before submit, deduplicate/lock the form submit, include form operations in global flushing, and preserve Session's defensive 409 boundary. | Closed — trusted after-evidence sends draft, blur-draft, and submit in order with 202/202/202, commits `rapid-form-value`, reruns the consumer, clears dirty/action state, and records no browser/runtime/stderr failures. |
| B-82 | High | CLI/Stop signal lifecycle | Three uncontended trusted-browser probes showed the first Stop on every fresh CLI worker accepted by one POST/202 yet completing normally after about 12 seconds; Stops 2/3 interrupted. A syscall trace proves `kill(correct_worker_pid, SIGINT)` returned 0 while that worker had `SigIgn=...0006` and no SIGINT catch bit. | The Unix launcher backgrounded its R CLI. POSIX asynchronous children inherit SIGINT/SIGQUIT ignored, and that disposition reached the first worker acknowledgement/evaluation window. Moving the acknowledgement inside `allowInterrupts()` could not override kernel `SIG_IGN`. | Foreground-`exec` the R CLI so it and every worker inherit normal terminal signal handling; retain request-scoped one-signal semantics and add a canonical installed-CLI three-Stop browser regression. | Closed — identical foreground-build replay interrupts all three trials in 0.490/0.451/0.264 seconds with one POST/202 and one correctly caught signal each, same-worker recovery, clean channels, exit 0, closed port, and no descendants. The complete affected widgets/outputs/Session/server/MCP/CLI files are green. |
| B-83 | Medium | Noninteractive execution / conversion | Cycle-4 release evidence `release-publishing/11-repeat-convert-policy-corrected.log` aborts because `alder_source()` executes an Rmd `eval=FALSE` cell; `14-repeat-convert-policy-final-corrected.log` records `converted-rmd-disabled-sentinel-executed=TRUE`. | `alder_source()` iterated the complete topological order and skipped only Markdown instead of applying Session's disabled-root plus transitive-descendant exclusion. `alder_test()` inherited the same mismatch. | Compute one deterministic runnable plan from disabled roots and reverse DAG edges; use it for both headless APIs, cover direct/transitive side effects and independent conditions/results, and document parity. | Closed in final v17 review — Disabled roots and descendants remain excluded during source/conversion; retained 17/17 installed regression and both current phases 06/11 agree. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-84 | Medium | MCP widget/reactive parity | Cycle-4 release evidence `release-publishing/17-mcp-parity-final.log` shows local MCP `set_widget` return with stale output 30 and `busy=false`; immediate `get_value(result)` also returns 30 before later pumping reaches 60. | The local MCP path waited only for runtime idle, so it could return in the idle window before the asynchronous widget response committed and scheduled consumers. Output-local operation records can also be replaced by one-shot reset operations. | Retain a bounded exact-token operation journal; wait for the initiating token to finish, propagate its error, then wait for its scheduled consumer run to reach idle. Regress automatic, lazy, no-consumer, reset-token, dependent-error, and operation-error paths through direct and stdio MCP. | Closed in final v17 review — MCP waits for initiating widget operations and consumers; focused token-error replay and both current phase 10 public-state results agree. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-85 | Medium | MCP one-shot interaction parity | Root's installed-candidate probe `cycle-4/integrated/root-boundary/00-run-button.log` records the first no-consumer `run_button` call returning success while its replacement reset token is `pending`, its visible value remains `TRUE`, and runtime reports idle. | The corrected waiter follows the initiating token and cell-execution busy state, but a run-button reset is a separate asynchronous worker operation that is not represented by runtime busy. | In automatic/app execution, link or otherwise follow the causally generated reset operation through terminal status and visible `FALSE`, propagating reset failure; retain lazy editor semantics where reset intentionally waits for a later explicit run. Regress no-consumer, consumer, immediate-repeat, and error boundaries. | Closed in final v17 review — Causal one-shot resets settle before returning; retained 50/50 replay and both current local/URL reset/repeat phases 10/14/15 agree. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-86 | Medium | MCP URL-backend parity | Corrected black-box replay `cycle-4/mcp-url-parity-before/02-probe-run-02-corrected.log` passes 18/26 checks but shows `alder_mcp(url=)` returning success with the initiating token still pending, old dependent output, later `widget_update_failed`, or an outstanding run-button consumer/reset. Local MCP waits correctly and every URL case eventually converges. | The URL adapter returns the REST endpoint's HTTP 202 acceptance directly; it has no lossless query for the Session's exact token journal and therefore cannot apply the local backend's settlement/error contract. | Expose a narrowly validated token-status boundary and make URL MCP wait for the exact initiating operation, automatic/app consumer idle, and causal reset; preserve lazy-editor deferral and Alder error codes. | Closed — the fresh installed artifact passes the identical 26/26 local/URL black-box checks, including initiating and reset settlement, exact `widget_update_failed` propagation, dependent-error state, lazy deferral, repeated buttons, clean protocol, closed ports, and zero live tracked processes. Focused MCP/server tests pass 154 expectations under `cycle-4/repairs/mcp-url-settlement-after/`. |
| B-89 | Medium | MCP URL run semantics | Immutable e0cb artifact replay under `cycle-4/repairs/mcp-url-settlement-after/04-run-scope-before-e0cb.log` records local counters `1→1→2` but URL counters `1→2→2`: automatic `run_stale` reruns a current side-effect cell, while lazy `run_all` does not rerun it. | Both URL tool names POSTed the browser's mode-sensitive `{all:true}` request, so the server selected `run_all()` or `run_stale()` from editor mode rather than the MCP tool requested. | Add a validated global `scope: all|stale` discriminator, retain mode-sensitive behavior when omitted for existing browser clients, and make the URL adapter state its intended scope. | Closed — corrected installed-artifact replay records matching local and URL counters `1→1→2`; automatic `run_stale` preserves the current cell and lazy `run_all` reruns it, all calls return idle, and cleanup is complete. |
| B-90 | Medium | Local and URL MCP run settlement | Installed queued-lazy evidence under `cycle-4/independent-mcp-url-after/` first proves an unrelated sleeper active, then records both local and URL `run_cell` returning success with its consumer done and runtime idle while reset token 8 remains pending and the visible run button remains `TRUE`; reset becomes done/`FALSE` about 0.9 s later. | `run_cell`, `run_all`, and `run_stale` waited only for generic runtime idle. Session schedules a lazy one-shot reset after the causal run's final cell terminalizes, and that separate worker operation is not represented by runtime busy. | Journal each exact run before dispatch, link every reset causally scheduled at its completion, terminalize only after execution plus all linked resets, propagate reset errors unchanged, and expose the bounded record to URL MCP through a narrow validated read-only route. | Closed — frozen installed URL and local active-sleeper replays each pass 11/11: both return only with the exact run and reset token done, visible value `FALSE`, consumer done, runtime idle, byte-identical source, and clean teardown. Complete Session/server/MCP files pass warning-free; evidence is under `cycle-4/repairs/mcp-run-settlement-after/`. |
| B-87 | High | Layout widgets / interaction integrity | Immutable installed-browser evidence `cycle-4/layout-widget-before/04-boundary-final.log` shows supported bare named widgets visibly rendered by `out$hstack()`, but a trusted slider update and run-button click both receive HTTP 400 `no such widget`. R state/output stay at 2/20 and FALSE, the DOM diverges to 3, a red error banner appears, and the run button remains disabled so it cannot be repeated. | Session widget discovery and mutation inspect only the final top-level `widget` record, while layout helpers wrap named widget records recursively under `layout$children`; client operation settlement likewise scans only top-level widgets. | Make widget discovery, mutation, operation/error/cancel/failure/reset handling, and client waiter reconciliation recursively address widgets in layout/lazy output trees while preserving sibling records and focused DOM identity. Regress every documented layout family, dependent error/recovery, run-button reset/repeat, and rapid continuous input. | Closed at focused scope by identical installed replay4; current exact v17 phase17 independently passes10/10 with four new actual PNGs, complete HTTP200 states and three accepted widget mutations. Slider2→3 yields20→30, Run resets/repeats, node identity/focus and clean channels/resources agree. See `release-completion/final-delivery-review/COLD-V17-1-VISUAL-REVIEW.md`. First v17 full cold now independently passes Status: OK/4,116 passes/zero other outcomes and strict terminal0/68 ports closed at06:46:22Z; see `release-completion/final-delivery-review/COLD-V17-1-TERMINAL-REVIEW.json`. Cold2 now also passes Status: OK/4,116 assertions/zero other outcomes and strict terminal0/all68 ports closed at07:33:40Z; final integration and26-flow closure are complete. See [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md). |
| B-88 | Medium | Widget rendering | The same installed browser starts `ui$slider(100, 110, value=103)` with public spec, `data-value`, and visible label 103 but the native range value/thumb at 100. | The browser assigns `input.value` while the range still has HTML's default 0–100 bounds, clamping 103 to 100, and applies the declared min/max afterward. | Apply min/max/step before value for scalar and range sliders, then assert native/spec/label equality across ranges outside browser defaults and after state reconciliation. | Closed at focused scope by identical installed replay4; current exact v17 phase17 independently confirms native/spec/label value103, correct min/max behavior and actual interaction with clean channels in the new four-image/state review. See `release-completion/final-delivery-review/COLD-V17-1-VISUAL-REVIEW.md`. First v17 full cold now independently passes Status: OK/4,116 passes/zero other outcomes and strict terminal0/68 ports closed at06:46:22Z; see `release-completion/final-delivery-review/COLD-V17-1-TERMINAL-REVIEW.json`. Cold2 now also passes Status: OK/4,116 assertions/zero other outcomes and strict terminal0/all68 ports closed at07:33:40Z; final integration and26-flow closure are complete. See [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md). |
| B-91 | Medium | Layout accessibility | Installed multi-layout evidence under `cycle-4/repairs/layout-widget-after/a11y-before/` visibly renders two tabs and two accordions, while its DOM snapshot records empty control IDs, duplicate global panel IDs (`tabs-0`, `tabs-1`, `accordion-0`, `accordion-1`), no tab roles/selected state, and no accordion expanded state after trusted pointer activation. | The layout renderer derived panel IDs only from layout type/index and toggled `hidden` without projecting semantic control state. | Allocate a unique ID namespace per rendered layout, link every control/panel, synchronize tab roles/selection and accordion expansion with visibility, then replay the identical installed fixture and real-browser file. | Closed at focused scope by identical installed replay4; current exact v17 phase18 independently passes12/12 and all six new actual images. Trusted pointer/arrow input, unique reciprocal IDs, roving selected/expanded state, isolated panels and identities across three polls agree; all recorded channels and owned cleanup are clean. See `release-completion/final-delivery-review/COLD-V17-1-VISUAL-REVIEW.md`. First v17 full cold now independently passes Status: OK/4,116 passes/zero other outcomes and strict terminal0/68 ports closed at06:46:22Z; see `release-completion/final-delivery-review/COLD-V17-1-TERMINAL-REVIEW.json`. Cold2 now also passes Status: OK/4,116 assertions/zero other outcomes and strict terminal0/all68 ports closed at07:33:40Z; final integration and26-flow closure are complete. See [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md). |
| B-92 | High | CLI/httpuv signal lifecycle | Immutable artifact `d69e6a95...b4f3` exits 130 and closes cleanly for idle SIGINT, but three fresh processes signalled during a proven-active 8 MiB `/api/state` request each return httpuv's generic HTTP 500 and remain alive with their ports open. Successful signal syscalls, catchable dispositions, active target-process state, and cleanup are recorded under `cycle-4/repairs/signal-during-request-before/`. | An interrupt raised inside httpuv's R request callback is caught and translated by httpuv before `alder_cli()`'s outer handler around `later::run_now()` can observe it; unlike B-82, SIGINT is neither ignored nor misdirected. | Catch interrupts around the complete request callback, latch a terminal interrupt in shared lifecycle state, defer safe teardown until the callback returns, and make the CLI return 130 from the latch. Retain idle SIGINT and worker Stop controls; add one installed active-request regression with port/descendant cleanup and no generic 500. | Closed at focused scope by identical installed replay4; current exact v17 phase16 passes16/16. Idle INT exits130; all three active requests return410, CLI exits130 and ports/descendants close, with no swallowed signal or generic500. See `release-completion/cold-v17-1/installed-audit/16-b92-signal-during-request.log` and `B92/`. First v17 full cold now independently passes Status: OK/4,116 passes/zero other outcomes and strict terminal0/68 ports closed at06:46:22Z; see `release-completion/final-delivery-review/COLD-V17-1-TERMINAL-REVIEW.json`. Cold2 now also passes Status: OK/4,116 assertions/zero other outcomes and strict terminal0/all68 ports closed at07:33:40Z; final integration and26-flow closure are complete. See [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md). |
| B-93 | Medium | Documentation/API drift | The 39fc artifact's API audit (`final-release-audit-39fc/02-api-docs.log`) found only 7 of 23 `ui` constructors documented and stale ADR claims for buttons/vector controls. | The exported `ui` collection and accepted widget contracts expanded faster than its generated help and ADR text. | Expand installed `?ui` documentation and correct the accepted widget ADR/API claims; strengthen the docs audit to check signatures and contracts, not only names. | Closed for the original drift: d83e corrected the stale widget ADR/API claims. The further expanded constructor documentation is closed under B-98; both current v17 installed API/help audits and full package checks verify the complete documented surface. |
| B-94 | High | R analysis breadth | The 4955 broad-R audit (`final-release-audit-4955/03-broad-r.log`) blocked statically bounded literal `assign()` and string-literal `rm()` despite allowing bare-symbol `rm()`. | The safety analyzer treated all mutation forms as unsafe instead of admitting bounded literal names while retaining computed/environment mutation guards. | Permit bounded literal assign/rm forms with actionable diagnostics for genuinely dynamic or explicit-environment mutation, and add direct broad-R regressions. | Closed. Exact v6 `03-broad-r.log` accepts literal `assign()` and both bounded `rm()` forms while still rejecting computed-name and explicit-environment mutation with actionable diagnostics; representative base, functions, pipes, formulas, namespaces, S3, tidy evaluation, literal eval/get/do.call/source, and ordinary Rscript execution pass. |
| B-95 | Low | Review infrastructure | The d83e audit harness exposed Chromote double-close warnings/zombies, same-basename artifact residue, blank-only stderr misclassified as dirty, and a self-inconsistent open-log evidence inventory. | Teardown, residual-file, stderr, and inventory finalization checks ran at unstable/open-file boundaries. | Make cleanup explicit and observable, distinguish intentional identical basenames and blank-only stderr, and finalize/hash the evidence inventory only after logs close; preserve failed attempts and require a fresh replay. | Closed as review infrastructure. Exact v6 cleanup reports zero live audit/Alder/LSP/Chrome processes, zero zombie delta, all 15 observed ports closed, no staging/temp residue, both full report captures and eight extracted images present. The finalizer independently re-verifies every path, size, and SHA-256 in its 220-file inventory. |
| B-96 | High | HTTP security boundary | The fresh runtime-security review found documented non-loopback mode accepts a forged allowed `Host` with omitted `Origin`, exposing `/api/state`, cell edits, and execution to unauthenticated network clients. Dynamic before evidence is under `cycle-4/repairs/security-boundary-before/observed-d83e-attempt2/`. | Origin/Host validation was treated as authorization, with no credential required on non-browser API routes. | Make non-loopback operation loopback-only until authenticated, or require a strong credential on every state/edit/run/package/file/lifecycle route and add authenticated browser/MCP regressions. | Closed at focused scope by exact v6; current exact v17 phase19 independently verifies non-loopback launch exit2, no readiness/token/execution and actionable loopback-host usage, while ordinary loopback lifecycle remains functional. See `release-completion/cold-v17-1/installed-audit/19-security-boundary.log`, `security-boundary/summary.json` and `root/COLD-V17-1-RUNTIME-API-REVIEW.md`. First v17 full cold now independently passes Status: OK/4,116 passes/zero other outcomes and strict terminal0/68 ports closed at06:46:22Z; see `release-completion/final-delivery-review/COLD-V17-1-TERMINAL-REVIEW.json`. Cold2 now also passes Status: OK/4,116 assertions/zero other outcomes and strict terminal0/all68 ports closed at07:33:40Z; final integration and26-flow closure are complete. See [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md). |
| B-97 | Medium | HTTP input/log integrity | The same runtime-security review found `/api/log` accepts valid wrong-shaped JSON and emits client-controlled multiline fields, allowing request errors and log-record injection; dynamic before evidence is under `cycle-4/repairs/security-boundary-before/observed-d83e-attempt2/`. | `/api/log` bypassed route schemas and treated message/source/URL/stack fields as trusted scalar text. | Add strict bounded scalar validation, reject arrays/objects/null with stable 400 responses, and strip/encode CR/LF/NUL before one physical log record. | Closed at focused scope by exact v6; current exact v17 phase19 independently verifies malformed log arrays/objects/null/unknown fields return400, valid multiline log returns200 and exactly one physical marker, shutdown202/exit0. Its intentional log marker is retained as fixture evidence. See `release-completion/cold-v17-1/installed-audit/19-security-boundary.log`, `security-boundary/summary.json` and `root/COLD-V17-1-RUNTIME-API-REVIEW.md`. First v17 full cold now independently passes Status: OK/4,116 passes/zero other outcomes and strict terminal0/68 ports closed at06:46:22Z; see `release-completion/final-delivery-review/COLD-V17-1-TERMINAL-REVIEW.json`. Cold2 now also passes Status: OK/4,116 assertions/zero other outcomes and strict terminal0/all68 ports closed at07:33:40Z; final integration and26-flow closure are complete. See [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md). |
| B-98 | Medium | Installed API documentation | The release review found `?ui` was an index rather than usable documentation for all 23 public constructors, values, constraints, and event semantics. | One collection-level roxygen topic did not describe its member APIs. | Add complete constructor signatures, accepted types, `$value` shapes, event semantics, and representative examples to installed docs and strengthen the audit. | Closed by exact v6 `02-api-docs.log`: all 23 constructor names, exact signatures/defaults, scalar/vector/composite value contracts, event semantics, ADR parity, source/mirror identity, and installed/source help parity pass. |
| B-99 | Medium | Compatibility/release testing | The release review found DESCRIPTION/README claim R >=4.1 while all execution evidence and CI validation start at R 4.6.1. | The compatibility floor was retained without a corresponding oldest-runtime job or revised declaration. | Add a real R 4.1 build/check/runtime job or raise `Depends` to the oldest supported and tested runtime, and align the docs. | Closed at historical v6 scope, which verified the then-declared4.6.1 floor. B118 supersedes that patch-level declaration with `R (>= 4.6.0)` for the compatible series; current v17 DESCRIPTION/packaged README/phase02 verify4.6.0, while local validation runs4.6.1 and R-devel/native Windows CI remain configured but unexecuted here. See B118 and `release-completion/cold-v17-1/installed-audit/02-api-docs.log`. Both current v17 package checks and complete cold gates now pass without warnings; B118 and final integration fulfill the release gate. |
| B-100 | Low | Packaged documentation | The release review found packaged README/help links to repository-only files omitted from the source package (`demo.R`, `dev/`, root review/rubric documents). | Repository and package audiences share documentation while `.Rbuildignore` excludes development material. | Qualify repository-only references with stable URLs or ship user-facing examples/docs under package locations. | Closed by exact v6 `02-api-docs.log`: the byte-identical packaged README uses stable links for the demo, developer guide, and decision index while excluded review/task files remain absent. B-104 separately covers unpublished acceptance-input URLs. |
| B-101 | Low | Installed API documentation | The first v4 artifact `d8c7fda4f0693335b9ad1292bef9d76786475860245433c8fa7a1372b3aca8d4` was invalidated at phase 02: installed `?ui` omitted the exact composite-name contract phrase `nonempty, valid UTF-8, unique name`. | The expanded `ui` topic documented the composite contract semantically but not with the audit's exact required wording. | Add the exact composite-name wording to `R/ui-widgets.R`, its worker mirror, and `man/ui.Rd`, then rerun focused documentation validation and rebuild. | Closed by exact v6 `02-api-docs.log`: the phrase is present, the full docs/API contract passes, and source, worker mirror, generated Rd, installed help, and artifact bytes agree. |
| B-102 | High | JSON parsing/resource resilience | Fresh v5 runtime/security review timing against valid installed-package JSON records `json_dup_key()` at 1,069B/0.023s, 10,069B/0.271s, 50,069B/10.507s, and 100,069B/>25s CPU-bound; `/api/upload` permits 16 MiB bodies, so the single httpuv loop can be monopolized. Follow-up also found jsonlite protection-stack risk on extreme nesting and lossy normalization of escaped NUL/unpaired surrogates. | `json_dup_key()` materialized every JSON string value one character at a time with repeated `paste0()`, producing quadratic allocation/copying before route validation; parsing without structural preflight also exposed recursive and dense-tree allocation hazards. | Parse once and detect duplicate decoded names in the unsimplified tree; add bounded pre-parse depth/container/member/quote/escape validation, preserve valid surrogate pairs and DEL, reject escaped NUL/unpaired surrogates, and prove near-ceiling parsing plus a decoded live HTTP upload without event-loop starvation or excessive RSS. | Closed by exact v6 phase 20. Fixed-size chunked preflight and parsed-tree duplicate detection pass exact boundary, escaped-key, UTF-8/surrogate, deep/dense, fuzz, chunk-edge, and 16MiB regressions. Fresh-process review records 0.39--0.81s and 252,000--271,000kB peak RSS (about 246--265MiB), 114--134MiB above loaded-source baseline including fixture copies. Exact v6 parses 16,776,192 bytes in 1.185s; real `/api/upload` accepts 16,771,824 bytes in 1.885s, stores exactly 12,578,814 decoded bytes, answers state in 0.320s, settles, exits 0, closes its port, removes the upload, and leaves zero descendants. Evidence: `cycle-4/repairs/json-resilience-after/REVIEW.md` and `final-release-audit-v6/20-b102-json-resilience.log`. |
| B-103 | Medium | Source-artifact verification | Fresh v5 release review RP-V5-2 found `dev/reviews/verify-source-artifact.R` removes `DESCRIPTION` from the equality vector but indexes the full candidate vector, causing logical-index recycling and potentially misattributing or missing byte mismatches. | The verifier's comparison result and candidate path vectors have different lengths. | Index one vector containing exactly the compared candidates and add first/middle/last negative-control mismatch tests. | Closed. One `byte_candidates` vector now drives comparison, indexing, and counts. The isolated regression passes exact v5 and an unmodified fixture, then rejects mutations in the first (`NAMESPACE`), middle (`R/b.R`), and last (`README.md`) comparison candidates with exact paths, reliable 5/4 counts, and exit 1. Root independently reran the complete regression after B-102 with the same result. Frozen v6 SHA-256 `6a00d96f289e0635f7efc6394bbf00ed39f056f14b19689846c99c5bbbf8485b` then passes 75/75 source inventory and 74/74 byte comparisons. Evidence: `cycle-4/repairs/source-verifier-after/00-regression.log` and `cycle-4/integrated/candidate-final-release-v6/05-content-verification.log`. |
| B-104 | Low | Packaged documentation publishing | Fresh v5 release review RP-V5-3 found packaged README links to `USER_FLOWS.md` and `NORTH_STAR_RUBRIC.md` return HTTP 404 because those local files are not yet published on `main`. | Package-facing links were updated before the repository documents were publicly published at the referenced revision. | Remove hyperlinks to unpublished acceptance inputs, label them as repository-source-checkout inputs, and retain/recheck the verified live repository links. | Closed by exact v6 `02-api-docs.log`. The artifact README labels `USER_FLOWS.md` and `NORTH_STAR_RUBRIC.md` as source-checkout inputs and contains neither unpublished URL; it is byte-identical to the reviewed repository README. The retained demo, developer-guide, and decision-index URLs each returned HTTP 200 in `cycle-4/repairs/readme-links-after/02-live-links.log`. |
| B-105 | High | Test execution / reproducibility | Fresh exact-v6 private-install probe uses one `test: true` cell to increment a file-backed counter and assert first execution; the marker ends at 2 and testthat records one failed expectation. | `alder_test()` first calls `alder_source(path, env)`, which evaluates every runnable code cell including marked tests, then evaluates each marked cell again through testthat. | Use one topological pass: evaluate non-test setup cells once and marked test cells once under testthat; retain disabled/transitive exclusion, condition fidelity, and dependency order; add a stateful regression plus subprocess exit-status evidence. | Closed by exact v7 phase 21: the file-backed marker remains 1, one testthat record is returned green, and the full replay plus focused warning-fatal file prove setup/test order, notebook-relative execution, disabled/transitive exclusion, classified failures/warnings, cleanup, and subprocess 0/1 status. Evidence: `cycle-4/integrated/final-release-audit-v7/21-b105-b108-release-safety.log`. |
| B-106 | High | Export/conversion data safety | Fresh exact-v6 private-install probes show `alder_export(path, ..., out = path)` and `alder_convert(path, out = path)` return success while changing the input MD5 and replacing its contents. | Only the implicit same-extension export default is disambiguated; explicit canonical/symlink/hard-link aliases are not rejected, and conversion/export serializers write directly to their requested destination. | Reject every same-file alias before executing input, stage output beside its destination, atomically replace only after complete validation, and regress same-path/symlink/hard-link aliases plus input and existing-output preservation on failure. | Closed by exact v7 phase 21: all six export formats and three converter inputs reject direct/normalized/symlink/hard-link aliases before execution, preserve input bytes and pre-existing destinations on failure, replace destinations on success, and leave no staging residue. Evidence: `cycle-4/integrated/final-release-audit-v7/21-b105-b108-release-safety.log`. |
| B-107 | Medium | Optimistic concurrency / API parity | Fresh exact-v6 probes send `expected_revision = 0.9` at real HTTP and local MCP boundaries; both return success, advance revision, and mutate `x <- 1` to `x <- 2`. | HTTP and MCP accept a general JSON number, then Session compares `as.integer(expected_revision)` to the stored revision, silently truncating fractions; overflow may also warn during coercion. | Require a finite, nonnegative, exactly integral value within R's integer range at HTTP, MCP schema and both backends, and Session; regress fractional/negative/overflow/non-finite/current/stale inputs with unchanged bytes/revisions and no warnings. | Closed by exact v7 phase 21 and complete warning-fatal Session/server/MCP files. MCP publishes bounded integer schema; invalid local/URL/HTTP revisions are rejected without mutation or warnings, current revisions succeed, stale revisions retain conflict semantics, and all owned processes/ports close. Evidence: `cycle-4/integrated/final-release-audit-v7/21-b105-b108-release-safety.log`. |
| B-108 | Low | Upload diagnostics | Exact-v6 `read_json_body(..., max_bytes = 16777216)` correctly rejects 16,777,217 declared bytes with 413 but reports `request body exceeds 1 MiB`; live upload evidence proves the active route limit is 16 MiB. | Both declared-length and streamed-overflow branches hard-code the default 1 MiB message instead of formatting `max_bytes`. | Derive the response text from the configured limit and regress both overflow branches at the ordinary 1 MiB and upload 16 MiB limits. | Closed by exact v7 phase 21: declared-length and streamed overflow report the active 1 MiB and 16 MiB limits exactly; phase 20 separately accepts and settles the real near-ceiling upload. Evidence: `cycle-4/integrated/final-release-audit-v7/20-b102-json-resilience.log` and `21-b105-b108-release-safety.log`. |
| B-109 | Medium | MCP framing / agent parity | Fresh exact-v7 raw-stdio probes sent conflicting duplicate `expected_revision` and `params.name` members; Alder selected the first value, returned success, and performed an unintended in-memory edit, while HTTP rejects duplicate keys. | The stdio loop and character-input dispatcher call `jsonlite::fromJSON()` directly without the HTTP boundary's bounded structural and recursive duplicate-key preflight; named R-list lookup selects the first duplicate. | Bound each raw MCP message, reject invalid UTF-8/NUL/structural floods and duplicate keys anywhere before dispatch, return deterministic JSON-RPC errors without mutation, and regress both revision orders, duplicate tool names/nested fields, survival, byte preservation, and local/URL backends. | Closed by exact-v9 phase 22. The installed raw-pipe probe passes 141/141 across local and URL backends, including exact 16 MiB acceptance, 64 MiB drain/recovery, both revision orders, duplicate/escaped/nested keys, malformed bytes/structure, strict IDs/params, unchanged revision/source, later valid requests before EOF, clean stderr/exit/port cleanup, and bounded parent RSS. Evidence: `cycle-4/integrated/final-release-audit-v9-rerun1/22-b109-mcp-framing.log`. |
| B-110 | Low | MCP JSON-RPC compliance | Fresh static defensive review found valid known-method requests without an `id` receive a JSON response, except for the one special-cased `notifications/initialized` method. | `mcp_dispatch()` returned directly from each known-method branch and checked `id_present` only for the unknown-method tail; it also treated an explicit null id as absent and accepted invalid logical ids. | Execute valid notifications but suppress every result/error response and reject invalid request IDs before tool effects. Cover direct and live-stdio notification behavior plus later request survival. | Closed by exact-v9 phase 22. Ordinary valid notifications execute without a response; invalid-parameter notifications stay silent without mutation; explicit null and every other invalid request ID receive `-32600`; a later ping survives in both local and URL modes. |
| B-111 | Low | MCP initialized notification | The exact-v8 failure replay returns no response for `{"jsonrpc":"2.0","id":7,"method":"notifications/initialized","params":{}}`; a conforming request peer can hang. | The method-name special case returns `NULL` before distinguishing an ID-bearing request from a genuine notification. | Accept `notifications/initialized` only without an ID; return `-32600` with an exact valid ID for request-shaped use, or null for an invalid ID, then prove a following ping survives locally and through the URL backend. | Closed by exact-v9 phase 22: genuine initialized notifications are silent, string/integer ID-bearing uses return `-32600` with exact IDs, invalid IDs return `-32600` with null, state remains unchanged, and both transports survive. |
| B-112 | Low | MCP request correlation | The exact-v8 replay accepts `id:null` with success and serializes fractional `id:0.123456789` as `0.1235`; phase 22 explicitly blessed the null-ID response. | ID validation uses looser JSON-RPC types, accepts every finite R numeric, and protocol JSON inherits jsonlite's four-digit default; large adjacent integers also collapse in binary64. | Enforce MCP string-or-integer IDs, reject null/fractional/non-finite/compound and numeric values outside inclusive ±(2^53-1) before effects, serialize numeric IDs with 17 significant digits for exact binary64 round trips, and replay safe endpoints, overflow adjacency, exponent forms, Unicode/empty strings, survival, and parity. | Closed by exact-v9 phase 22: both transports accept empty/Unicode strings, -1/0/1, exponent-form integers, and both safe endpoints exactly; reject null, fractional, logical, compound, out-of-range, and nonintegral-exponent IDs before mutation; and preserve local/URL response parity. |
| B-113 | Low | MCP parameter schemas | Exact-v8 returns success for `ping` with scalar `params:false`, and its own initialization flows use incomplete `params:{}` despite required protocol version, capabilities, and client information. | Only `resources/read` and `tools/call` call the object-params validator; simple/list/lifecycle methods bypass generic and method-specific validation. | Centralize structured params validation, require named objects for MCP methods, validate the required initialize fields, and reject invalid request params with `-32602` before effects while keeping invalid notifications silent. Replay direct and live local/URL matrices plus post-error survival. | Closed by exact-v9 phase 22 and integrated phase 10. Scalar/null/array ping params and incomplete or mistyped initialization fields return `-32602` without mutation; valid named-object initialization negotiates Alder identity and capabilities in both local and URL modes. |
| B-114 | Low | Release tooling / lint | On a clean library after removing an accidental global Alder install, `run-static-gate.sh` emitted package-resolution warnings and hundreds of false object-usage findings; the same source linted cleanly after explicitly loading the source namespace. | The lint gate implicitly relied on a globally installed copy of the package to resolve exports and cross-file symbols. | Load the checkout namespace explicitly before `lintr::lint_package()` and rerun the complete static gate without any installed global Alder. | Closed — `09-static-gate-final.log` passes dependency audit, byte-identical editor rebuild, JS/R/YAML/shell/batch validation, ShellCheck, worker mirror identity, and namespace-loaded package lint with `No lints found` and `static gate: OK`; no global Alder is installed. |
| B-115 | Low | Release tooling / MCP fixtures | The first exact-v9 integrated audit stopped at phase 10 because its accepted parity fixture encoded `capabilities = list()` as JSON `[]`; v9 correctly rejected that incomplete initialize request with `-32602`, and cleanup subsequently reported the expected missing downstream image extracts. | R's empty list is serialized by jsonlite as an array unless it retains an explicit empty names vector; older fixtures predated method-specific MCP parameter validation. | Encode empty MCP objects as explicitly named empty lists in every active parity fixture, replay the phase-10 flow and B-86 local/URL flow against exact v9, then rerun the complete audit into new evidence. | Closed — focused exact-v9 phase 10 passes, B-86 passes 26/26 across local and URL backends, and the fresh 22-phase audit completes with all eight images, zero live owned processes, all 15 observed ports closed, no zombie increase, and a reverified 227-file inventory. Evidence: `cycle-4/repairs/mcp-audit-harness-after/` and `cycle-4/integrated/final-release-audit-v9-rerun1/`. |
| B-116 | Low | MCP lifecycle / data safety | Fresh exact-v9 runtime/security replay sends `tools/call add_cell` as the first wire frame and receives success; the later initialized notebook state contains the unintended cell. | Both MCP backends set an `initialized` flag but the common dispatcher never consulted it, and the 141-case framing probe always initialized before exercising tools. | Model `new` → awaiting-initialized → ready explicitly; allow only ping before readiness, reject pre-ready requests deterministically, keep pre-ready notifications silent and effect-free, and regress malformed/early/repeated handshake frames plus mutation survival over local and URL stdio. | Closed in final v17 review — Deferred startup and clean MCP lifecycle retain readiness and pre-ready behavior; current complete MCP source 830/40 and both phase 22 suites agree. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-117 | Low | MCP tool-call schema | Fresh exact-v9 replay accepts array-valued `arguments`, missing/numeric/unknown tool names, and reports envelope/schema failures as successful results with `isError:true`. | Dispatcher validation stopped at the outer `params` object and delegated wire-schema failures to the tool execution/result layer. | Require a known nonempty string `name` and object-valued `arguments` before invocation; return top-level `-32602` for malformed requests, keep malformed notifications silent/effect-free, and reserve result-level errors for failures after a valid tool invocation reaches execution. | Closed in final v17 review — MCP request-envelope/schema rejection remains exact at the wire boundary; current source 830/40 and both phase 22 suites agree. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-118 | Medium | Package metadata / release gate | Independent exact-v9 `R CMD check --as-cran` emits `Dependence on R version ‘4.6.1’ not with patchlevel 0`; CI declares any warning fatal. | DESCRIPTION used the tested patch release as the dependency floor, although R package dependency metadata expects a series floor ending in patchlevel zero. | Declare the compatible R 4.6 series as `R (>= 4.6.0)`, retain explicit R 4.6.1/devel validation, align user documentation, and prove the exact successor package check is warning-free. | Closed in final v17 review — DESCRIPTION and README use R >= 4.6.0 while the tested local release is R 4.6.1. Both exact current package checks are warning-free; B99 remains historical 4.6.1 scope. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-119 | Low | Release tooling / MCP fixtures | The first exact-v10 integrated audit passed initialization at phase 10, then reported empty state and 11 cascading failures while the standalone 215-check protocol preflight was green. | The active parity fixture serialized omitted object-valued `tools/call.arguments` as an unnamed R `list()`, producing JSON `[]`; strict v10 correctly rejected that request with top-level `-32602`. | Encode omitted tool arguments as explicit empty JSON objects in every active MCP audit fixture, preserve the failed attempt, replay phase 10 against the same artifact, then rerun the complete audit in fresh evidence. | Closed as review infrastructure — all six active fixtures parse with explicit empty objects and the exact-v10 phase-10 replay passes every assertion with clean protocol/shutdown. Evidence: `cycle-4/integrated/final-release-audit-v10/10-cycle4-mcp-parity.log` and `cycle-4/repairs/mcp-audit-harness-v10-after/01-phase10-mcp-parity.log`. |
| B-120 | High | Headless publishing / condition fidelity | Fresh exact-v10 probes publish a notebook with blocking syntax diagnostics successfully: direct Pandoc and Session-backed HTML export replace a sentinel destination, omit the blocking diagnostic, and may display invalid source without its error. | Headless Session startup converts `graph_invalid` into interactive state, the idle waiter ignores blocking diagnostics and `last_action_error`, and serializers reject only runtime-error cell states. | Reject every blocking analysis condition before user-code execution or destination replacement across direct rendering and Session-backed exports; preserve complete diagnostics, effects, existing destinations, and staging cleanliness for syntax, duplicate-definition, cycle, and bounded-dynamic failures. | Closed in final v17 review — All four blocking graph conditions are complete and reject before effects/staging across six exports and both renderers; each current phase 21 passes 160 B120 assertions within 262. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-121 | Medium | Publishing data safety | Fresh exact-v10 `alder_render(..., engine = "pandoc")` accepts a hard-linked output alias, executes the notebook's marker side effect, and only then replaces the alias. | `alder_render()` compares normalized path strings but does not use the existing inode-aware `alder_same_file()` guard used by export and conversion. | Reject direct, normalized, symlink, and hard-link output aliases before reading or executing notebook code for both supported render engines; preserve source/destination bytes and leave no staging residue. | Closed in final v17 review — Both render engines reject direct/canonical/symlink/hardlink source aliases before mutation; each current phase 21 passes 32 B121 assertions within 262. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-122 | High | Local MCP startup / agent parity | Fresh exact-v10 local MCP executes a marker before the first protocol frame, reports hard-coded automatic/startup-true runtime state despite explicit lazy/startup-false notebook policy, and diverges from the URL backend. | The local MCP context constructs a Session with hard-coded policy and pumps startup to idle before beginning stdin/protocol processing. | Resolve the same notebook runtime policy as CLI/URL, keep pre-ready and failed/abandoned negotiation effect-free, trigger any requested startup only after the valid initialize/initialized transition, and regress all mode/startup combinations plus early EOF, latency, failure, and cleanup parity. | Closed in final v17 review — MCP defers startup to negotiation and preserves automatic/lazy × startup policy, exactly-once execution and recovery; both current 286-check phase 22 matrices agree. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-123 | High | Quarto reactive publishing | Fresh installed-source review renders a valid notebook with `answer <- base_value + 2L` displayed before `base_value <- 40L`; Pandoc succeeds, but Quarto exits 1 with `object 'base_value' not found`. | Generated knitr chunks execute in display order rather than the reactive DAG; disabled descendants also need the shared runnable graph. | Execute each enabled cell once in DAG order through native knitr while preserving authored report order, conditions, and plots. | Closed in final v17 review — Quarto executes enabled cells once in DAG order while preserving authored report order and disabled descendants; focused native review and both real-engine publication phases pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-124 | Medium | Browser error telemetry | Trusted LSP-death/retry review passes 29/30 checks; `/api/log` returns 400 for the frontend's actual `{level,message,source,code,status}` failure report. | Frontend API failures include structured code/status fields absent from the strict server schema. | Accept and bound these optional fields, emit them in the sanitized single-line log, and verify the real recovery flow plus malformed input rejection. | Closed in final v17 review — The browser telemetry schema accepts bounded real code/status fields and retains hostile/multiline rejection. Focused 14/14 plus 13/13 and native 30/30 recovery are supported by both current security/browser gates. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-125 | Medium | Composed scientific outputs | Fresh browser review displays a model summary normally as a standalone value, but `out$tabs(summary(fit))` shows a shallow structure dump; a string callout shows `chr` inspection text. | Layout construction eagerly converts general values through `utils::str()` instead of the worker's scientific renderers. | Share normal value rendering with layout/lazy output while retaining named widget identity; keep explicit structure inspection in `out$inspect()`. | Closed in final v17 review — Composed/lazy values use the same scientific renderer as standalone output; focused composition 39/output 52/streaming 32 and both fresh nested/ARIA/scientific reviews pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-126 | High | Agent scientific-data integrity | After an upstream edit, MCP notebook state labels the dependent stale, but `get_value` succeeds with the old numeric result and no freshness metadata. | Value inspection exposes worker bindings without checking source ownership/revision freshness. | Reject stale value inspection with an actionable diagnostic across Session, HTTP, and MCP; retain clearly labelled historical cell outputs and restore normal inspection after rerun. | Closed in final v17 review — Session-owned inspection rejects stale and late-invalidated values across HTTP/local/URL MCP while retaining labelled historical outputs. Focused 90/90 plus 39 direct regressions and current full gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-127 | High | Notebook-native publishing fidelity | Public Quarto rendering succeeds but turns `out$md`/layouts into internal record dumps and loses nested plots and model printing; Pandoc flattens callout Markdown and nested images to plain text. | Quarto has no native Alder output print bridge, and Markdown export falls back to shallow plain-text serialization for layouts. | Serialize composed outputs recursively and add a Quarto-scoped native knitr print/render bridge; preserve headings, labels, images, model output, conditions, metadata, and once-only cell execution. | Closed in final v17 review — Both publishing engines retain recursive Markdown/layout/plot/model/widget rendering and ordered conditions. Focused 10/10 per engine,38 condition/append checks and both current report/embedded-byte reviews pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-128 | High | Worker package identity | Public export with `library(alder, lib.loc = private)` uses the current parent package but silently loads the older site-library Alder in its worker. | Vanilla Rscript does not inherit the parent's loaded-package location or `.libPaths()`. | Bootstrap the worker from the same installed Alder library, retain dependency lookup and subsequent project/sandbox isolation, and regress a parent with cleared library environment hints. | Closed in final v17 review — Worker package identity follows its installed parent while preserving dependencies and sandbox isolation. Public before/after,5/5 regression,9/9 sandbox evidence and current full worker-library gates agree. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-129 | High | Static export condition fidelity | Public HTML/Markdown exports omit captured warnings/messages; runtime-error exports report success and replace sentinel destinations. Session JSON contains source text but omits structured runtime logs/errors. | Document serializers ignore cell logs and headless export validates analysis but not terminal execution failure. | Retain ordered conditions in HTML/Markdown and structured logs/errors in JSON; reject failed execution before staging with complete state and preserve destinations across all six formats. | Closed in final v17 review — All six static exports retain ordered complete conditions and reject runtime failures before replacing destinations. Focused 66 assertions and current full export-condition/publishing gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-130 | Low | Review fixtures / supported R and native HTML | The full source suite records7 failures: four browser and one dataflow assertion expect literal assign to be prohibited; two publication assertions reject writer-added boundary newlines around otherwise exact headings. | Old fixtures predate supported literal assignment and semantic heading rendering. | Use a computed assignment target for dynamic-analysis fixtures and trim only heading boundary whitespace while retaining exact label order/count and every behavioral assertion. | Closed in final v17 review — Fixtures now exercise supported literal assignment and compare semantic heading labels without weakening order/count; focused dataflow 73/publishing 34/editor 32 and current full suites pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-131 | Medium | Language-server project identity | Direct URI/configuration probes decode Alder's `file://%2Ftmp...` URI as an empty path. A notebook-local empty linter list is ignored; canonical URI honors it. The URI-only repair still fails the nonexistent-notebook project-policy test. | Path separators are percent-encoded and the language-server child inherits the launch directory, which is used for missing-file configuration lookup. | Preserve native filename identity in file URIs and start the language server in the notebook directory; verify escapes, Unicode, Windows forms and existing/new notebook policy. | Closed in final v17 review — Native LSP URIs and notebook-local launch directory preserve existing/new notebook project policy. Focused 17 path assertions and current full LSP/browser gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-132 | High | Editor condition fidelity | Traced LSP publishes severity-1 `Failed to run diagnostics` at (0,0), but Alder exposes no diagnostic and no service failure. The upstream lintr 3.4.0 object-usage linter fails on valid call-computed assignment. | Cell-only translation drops delimiter, cross-cell and unlocated diagnostics, silently hiding the upstream failure. | Retain document-level diagnostics with exact message/severity/original file range, expose them in Session/API state and an accessible editor region, clear on replacement or lint-off even after LSP death, and keep them nonblocking. | Closed in final v17 review — Document-level LSP diagnostics retain complete messages, levels and source range, remain nonblocking, and clear on replacement/off. Focused LSP70/browser 32 and current diagnostics/full gates pass; the upstream lintr defect is surfaced, not claimed fixed. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-133 | Medium | Live editor synchronization | B-132's browser after-replay passes display/severity/off and normal lint policy, but the recovery assertion retains old header diagnostics after a successful ordinary cell edit to revision 1. | The server synchronizes LSP text only during formatting or another language-help request; state polling maps old results without sending normal edits. | Synchronize the current notebook on state polls, clear obsolete source diagnostics, ignore outdated versioned publications, preserve visible sync failures, and require fresh positive diagnostics after an ordinary edit. | Closed in final v17 review — State polling synchronizes ordinary edits and rejects obsolete diagnostic versions. Focused LSP70/editor 32 with fresh positive notes and current source/installed/browser gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-134 | Low | Browser review fixture / diagnostic visibility | The full restored-environment source run completes 3,594 expectations across 377 tests with exactly two failures: initial and idle syntax waits in the transient-diagnostic browser case; errors/warnings/skips are zero and strict resources are clean. | Four unscoped single-node queries now select the newly added notebook-level diagnostic region, so text waits read the wrong node and intermediate hidden checks pass vacuously. | Scope the existing case to its cell diagnostic node, require initial/idle visibility from that same node, retain all timing/assertions and aggregate lint-policy checks, and replay identical inputs with independently recorded cell states. | Closed in final v17 review — The browser fixture observes its explicit cell node and visibility; identical before two failures becomes three passing explicit checks. Current full suites pass; the distinct production freshness fix remains B135. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-135 | Medium | Editor diagnostic freshness | Directly inspected `transient-diagnostics/replay1/after/idle-syntax-diagnostic.png` shows `y <-` in the editor but a visible cell diagnostic quoting `x <-`; the same-cell trace records that old message at 1,018.5ms after the edit. | The one-second diagnostic timer re-renders lastState without checking source generation/revision or displayed source identity. | Show cell and document notes only for current displayed/acknowledged source; keep obsolete notes hidden through delayed edit/state delivery, then show the exact new-source condition. | Closed in final v17 review — Displayed diagnostics require current displayed/acknowledged source through delayed edit/state delivery. Focused before 10 failures to after 63 passing assertions and both current diagnostic/full browser gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-136 | Medium | Native hover help / visual and keyboard access | Actual native mean hover covers nearly the entire 1440×1000 notebook with raw Markdown/HTML; text overflows its background across other cells in `editor-command-review/attempt1/10-native-hover.png`. | Hover content is an unbounded preformatted raw upstream document with no dedicated size/overflow treatment. | Retain full help in a safe, readable, bounded scrollable panel with responsive geometry, keyboard open/dismiss, and focus return. | Closed in final v17 review — Full native help renders safely in a bounded scrollable desktop/narrow panel with keyboard dismissal/focus. Focused 6 native plus 20 rendering assertions, current native 34 refresh and full help/browser suites pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-137 | High | Formatting / source and revision synchronization | Native Ctrl-Shift-F changes public server source to `numbers <- c(1, 2, 3)` but the focused editor retains `numbers<-c(1,2,3)` after 20 seconds. Its next native edit receives HTTP 409/source_conflict in the same tab; screenshots and API state are retained. | Focus protection refuses the explicitly formatted body/revision, so later edits use stale acknowledged source identity. | Return authoritative source/revision receipts for formatting with revision preconditions; adopt the acknowledged transformation for shortcuts/save/autosave while preserving newer local typing, selection and genuine external conflicts. | Closed in final v17 review — Formatting returns authoritative revisions and adopts acknowledged changes while preserving newer typing, selection and real conflicts. Focused 37 backend/35 browser plus current native 34 source/disk receipts and full gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-138 | High | Shared Markdown HTML sanitization | A direct retained-source probe returns event/style/autofocus/form-action attributes unchanged on unknown wrappers. New regression output also retains nested script/iframe/input/form elements and duplicates unsafe text. | The unknown-tag branch calls xml_set_text without sanitizing descendants, removing child nodes, or stripping wrapper attributes; libxml2 does not flatten the subtree as assumed. | Recursively sanitize children, remove all remaining content nodes before assigning escaped inert text, and strip every unknown-wrapper attribute while preserving safe content and URL restrictions. | Closed in final v17 review — Markdown sanitization removes dangerous descendants and unknown-wrapper attributes while retaining inert text. Meaningful original 6/13 failures become 13/13; full current markdown/hover/browser/security suites pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-139 | High | Immediate keyboard save / work safety | A real held-state browser probe edits a clean notebook to `value <- 10` and immediately presses Ctrl-S while Save.disabled is true. The edit request succeeds, no save request is sent, and disk retains `value <- 1`; two assertions fail with strict clean resources. | Editor Ctrl-S calls the disabled Save button's click method, so the command depends on an unrelated state poll enabling that control. | Invoke the shared save operation directly from the keyboard, serialize overlapping save requests, flush edits and optional formatting before writing, and prove immediate typing/format/save and autosave preserve current source on disk. | Closed in final v17 review — Immediate Ctrl-S invokes shared serialized saving before the toolbar poll. The retained no-request/stale-disk failure becomes exact saved current source; focused 2/2 plus 16/16 and current native 34/full save gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-140 | Medium | Retained action-error provenance | Direct after04 screenshot shows fresh inline syntax text for `pending_problem <-` beside an unqualified red global error quoting `old_problem <-`. The global condition is the retained previous run failure, not a current-source diagnostic. | renderStatus presents Session.last_action_error as plain current-status text without a historical label. | Retain the complete error and identify it explicitly as a previous failed action; verify the exact delayed-edit scenario and fresh inline note independently. | Closed in final v17 review — The complete retained prior action is labelled Last failed action while inline/header notes match current source. Exact before one failure becomes 64/64; both fresh diagnostic groups and full browser gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-141 | Medium | Keyboard R help / asynchronous delivery | After a desktop-to-narrow transition, a real F1 event at character2 receives HTTP200 with complete rendered mean help, yet no tooltip appears. Same installed replay repeats the loss with strict clean resources. | CodeMirror hover pending results are discarded on view updates and the retry uses the last mouse position, losing explicit keyboard intent. | Preserve a guarded explicit keyboard-help request across harmless view updates; reject obsolete source/selection/focus or canceled intent and activate the completed help synchronously. Verify delayed real response, view changes, cancellation and normal native controls. | Closed in final v17 review — Explicit F1 delivery survives harmless view updates and rejects obsolete/canceled intent. Meaningful retained bundle failure becomes 29/29, followed by current native 34/twelve-image review and full installed browser gates. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

## Final cold-source recovery finding

| ID | Severity | Subsystem | Visible evidence | Likely root cause | Actionable correction | Status |
| --- | --- | --- | --- | --- | --- | --- |
| B-142 | Medium | Worker startup / barrier recovery / integration reliability | Frozen v11 cold1 source gate reports test-session.R1313 recovery timeout after10s:3742 expectations393tests, one error, no failed assertions/warnings/skips; strict cleanup passes. Exact-input20case replay retains deadline breaches but every case eventually reaches bothdone on the same worker. First timeout has matching pending/current identity, active polling and no readable response; worker's first acknowledgement follows roughly9s startup. | Rscript file-mode runtime loading takes about8.8s. Whole-file eval(parse()) reduces readiness to about1.1s while preserving R options and JIT; no lost response or scheduler race is established. | Profile package load/runtime construction/dispatch; correct the demonstrated cause while preserving user R/JIT behavior and an explicit recovery contract, then run unchanged-input regression and full frozen gates. | Closed in final v17 review — Complete private worker bootstrap removes the observed readiness delay: identical 20 cases have zero original-deadline breaches after 7/20 before. Compatibility 544/56 and current full bootstrap/lifecycle gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |


| B-143 | Low | Malformed-bootstrap regression observation | Cold v12 source completed 3,836 expectations/396 tests with two failures at test-worker-bootstrap.R138–139: complete parse error and dead/nonzero process were present, while the private failure flag was unset and one request awaited the next poll. All other tests passed; strict resources were clean. | The new test assumed synchronous private bookkeeping, although readiness may observe process death between transport polls and an already-dead send may return its error synchronously without that flag. | Retain the actual parse/error/exit assertions and verify exactly-once callback identity and eventual request drainage through normal event-loop pumping; separately probe any public stale-state concern. | Closed in final v17 review — Malformed-bootstrap tests observe asynchronous readiness settlement after polling; callback-once/pending-empty and complete diagnostics remain asserted. Focused bootstrap 103 within 544/56 and current full gates pass; B144 remains the separately repaired public defect. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-144 | High | Failed Restart R / runtime freshness | Exact installed-v12 probe warms x=2/y=6, inspects y and fails the replacement worker with a malformed runtime. Four of ten public Session restarts retain done/done cells, variable metadata and inspected y=6 after the pending request drains, despite worker_available=false and a complete startup error. Six cases stale correctly, demonstrating timing dependence. | The restart catch sets worker_failed before the normal failure callback can invalidate state; the successful path alone clears old values and stales cells. | Invalidate the old runtime view before replacing its process and repeat cleanup after success/failure to prevent old pending callbacks restoring inspection state; retain output/source bytes and allow editing/save/recovery. | Closed in final v17 review — Restart invalidates runtime before replacement: ten public after cases retain exact stale outputs/source/revisions and clear old inspections, and 54/54 permanent checks prove edit/Save/later replay to fresh y=9. Focused 544/56 and current full recovery gates pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-145 | Medium | Release audit environment / ordinary DBI fixture | Frozen v13 first cold passes the full source/browser suite (3,901 expectations/398 tests, zero other outcomes) and installed phases 01–10, then phase 11 stops at duckdb::duckdb(): no such package. Overall cold exit/resource is 1/0 with strict zero resources; five actual diagnostic images pass scoped independent review. | Environment restoration verified its selected 105 versions/39 namespaces but omitted the excluded audit fixture's DuckDB backend; Imports/Suggests checks do not cover every review-only package. | Restore the missing backend with exact provenance, replay the unchanged failed DBI notebook, check the remaining held fixture dependencies, then complete two fresh cold cycles using the unchanged v13 archive. | Closed in final v17 review — The omitted review-only DuckDB dependency is restored with retained official binary/version/hash provenance. Exact notebook returns a=3/b=4 under warn 2 with unchanged source/closed connection, and both current phase 11 DBI fixtures pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-146 | Low | Installed audit expectations | Frozen v13 first retry passes installed phases 01–20 and all 25 scoped actual-image reviews, then phase 21 fails exact equality against the fragment `unexpected end of input`; the actual condition includes native location, source and caret. After correcting that expectation, replay1 prints all 262 semantic PASS records and fails the stale total of 230. Both failures retain main/resource 1/0 and strict zero resources; neither reaches phase 22. | The excluded fixture compared a fragment to a complete diagnostic record and retained an assertion total inconsistent with its current loops. These are audit expectations; actual product diagnostics and data safety are correct. | Derive the expected complete syntax condition from native base R parsing of the same fixture, retain strict message-record equality and all semantic assertions, set the independently counted total to 262 with its breakdown, and rerun full phases 21/22 followed by both fresh cold cycles against unchanged v13. | Closed in final v17 review — Audit expectations now match the complete native parse diagnostic and all 262 semantic records; no assertion was removed. The held active-input snapshot and both current phase 21 transcripts confirm 262/262. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-147 | Low | MCP startup audit fixture / evidence accounting | Exact-v13 focused replay2 phase 22 records 283 PASS and two FAIL startup checks, then stops at expected 286/actual 285 before writing detailed JSON. Raw before2 observation verifies all 12 diagnostic facts: successful notebook_state retains the complete ordinary cell error with an available idle worker, the marker literally contains started, and the old startup function records four checks. | The fixture expected an ordinary startup cell error to become a protocol settlement error, numerically parsed a text marker, and expected five records from four calls. The early count guard also hid detailed failed records. | Preserve the actual successful-state cell-error contract, use a numeric execution counter, add revision-aware edit/run/state/fresh-value recovery as the fifth assertion, and serialize detailed records before the unchanged fatal total-286 guard. Retain genuine startup-settlement error coverage and all other wire checks. | Closed in final v17 review — The MCP startup fixture observes a numeric exactly-once counter, ordinary cell-error state and revision-aware recovery 42 with full condition. Focused 12/12 diagnosis,five repaired checks and both current 286-check phase 22 runs pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-148 | Low | Installed export-test subprocess fixture | Cold v13 first-cycle second retry passes all 22 installed audits and 25 actual-image reviews, then full R CMD check reports two failed assertions in the alder_test subprocess test: success exits 1 and the failing notebook lacks its expected condition because pkgload cannot find DESCRIPTION in alder.Rcheck. Overall suite is 3,895 PASS/five FAIL/zero WARN/SKIP; the other three failures are B-149. | The test derives a source root by walking above test_path, although installed checks relocate tests outside the installed package; both children fail before notebook execution. | Resolve the loaded Alder namespace path, preserve parent dependency libraries, select the actual source or installed loader and verify child namespace identity while retaining all three original status/condition assertions. | Closed in final v17 review — The export subprocess fixture works from source and installed package roots while preserving good/bad notebooks and original status/full-condition checks. Complete changed-source and exact installed 314/21 focused gates plus both full current suites pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-149 | Low | Installed worker-library test prerequisites | The same full installed v13 check reports parent status1, nonempty load-error stderr and absent output in test-worker-library.R: Alder cannot load because R6 is absent from the vanilla child search path. The worker is not yet created. | R CMD check injects dependency libraries through startup.Rs; the fixture clears R_TESTS and library environment hints without carrying the parent library search path into the separate parent process. | Restore the parent process dependency .libPaths through an RDS fixture while preserving empty R_LIBS/R_LIBS_USER/R_TESTS and --vanilla; leave the real worker to resolve Alder and dependencies through product bootstrap and retain all original assertions. | Closed in final v17 review — The worker-library fixture clears the intended parent hints without assuming an obsolete private R6 member. Source/installed 314 focused evidence and 54-check control/fault/fresh-control prove the original five assertions still falsify missing worker-library propagation; both current full suites pass. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |


| B-150 | High | Worker late-interrupt boundary / trusted slider final gate | Exact v14 cold1 completes 22 audits and 25 reviewed images, then full installed check records 3,895 PASS/10 FAIL/zero WARN/SKIP in one trusted-slider focus/identity case. Separately, corrected sleep-free delayed-before2 exposes the existing parser gap: a late matching native SIGINT kills the original worker after completed42, so queued ping/fresh43 fail. | Late delivery of a real started ACK can trigger its matching signal after the worker advances beyond evaluation. Sleep-free before2 confirms that exposed CPU parser boundary. The original browser failure is consistent with this mechanism but its causal attribution remains unconfirmed without a failing native trace. | Protect protocol/parser boundaries, drain an older pending signal before dispatch, retain request-scoped evaluation Stop and complete conditions, and prove meaningful original/final/no-drain regression plus both complete cold gates. | Closed in final v17 review — Protocol parsing/transport and dispatch retain identity across late interrupts while active R evaluation remains interruptible. Sleep-free 17/17,permanent 31/31 with original/no-drain negatives,pure-R19/19 with no-allow negative,and focused 745/78 pass. Current native slider replay and full source/installed suites pass. The original slider cold event remains consistent with, but not conclusively attributed to, the proven worker race; v 1 injected-sleep observer limitation remains preserved. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
| B-151 | High | Dataflow navigation focus / final source gate | Exact v15 first cold fails the unfiltered source/browser suite: 3,952 expectations/400 tests, three assertion failures, zero errors/warnings/skips. In browser outline handles trusted Enter and Space once, Enter navigation and the third-link immediate focus check pass, then the Space wait times out (2439), only Enter is recorded (2444), and only the second-cell navigation is recorded (2447). | Focused real-poll evidence confirms Outline replacement loses keyboard focus: same version7 state disconnects the focused button and leaves BODY active; a real rename to version8 updates the label while again replacing the focused node. renderOutlinePanel creates fresh controls on every renderDataflow. The original cold lacks its own failing DOM/event trace, so that exact occurrence remains consistent with, rather than conclusively attributed to, this mechanism. | Preserve original native Enter/Space, source, waits and exactly-once assertions. Capture actual focus/node/event/navigation state, resolve the demonstrated cause and prove focused plus complete final source/browser/cold acceptance. | Closed in final v17 review — Keyed Dataflow reconciliation preserves focused nodes/graph scroll through polls,renames and actual reorder/add/delete. Focused 23+69+67 passes and the anchor mutant meaningfully fails; current native 34/twelve-image and permanent 247/eight-case reviews pass, followed by complete source and both installed browser suites. The uncaptured original Space event is not conclusively attributed; failed order 1 fixture assumptions remain preserved. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-152 | Medium | Final audit startup evidence / phase22 loopback readiness | Exact v16 first cold passes the full source/browser suite4110 expectations/403 tests with zero other outcomes, then installed phase22 prints109 local PASS checks and stops with loopback Alder server did not become ready / Execution halted. Main exit1, resource0, all62 observed ports closed; full installed package check not run. | Underlying server readiness cause is unknown. Confirmed audit diagnostic loss: start_server discards curl/JSON conditions, conflates child exit and60-second timeout into one message, and stops before recording the process identity/exit/pipes/last response. Final framing JSON is written only after later work, so this attempt retains an empty b109 evidence directory. | Preserve the failed attempt. Capture the same native startup/readiness sequence passively, including complete conditions, process identity/exit/stdout/stderr and last public response before cleanup; keep all286 assertions and60-second deadline. Diagnose and correct only the demonstrated cause, then complete both final cold gates. | Closed in final v17 review — The excluded audit now retains exact startup source,HTTP conditions,pipes/process state and an uncaught observer-error guard. Meaningful child-exit 73 negative and both current 286-check/five-ready-capture runs pass without relaxing the 60s deadline. The original unrecorded loopback readiness cause remains unknown; this closes the demonstrated diagnostic-loss finding, not a claim to have identified that event. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |

| B-153 | Medium | Local MCP widget settlement / installed test response boundary | Exact v16 retry passes all22 installed audits and25 reviewed images, then full installed check reports4098 PASS/1 FAIL/0 WARN/SKIP. The only error is test-mcp.R:1234, local MCP set_widget settles with no consumers: mcp_tool_payload passes a missing/invalid txt to jsonlite::fromJSON. Driver main1/resource0; all68 observed ports closed. | Controlled comparison proves a fixture hazard: timeout2 applied before the first handshake also bounds deferred notebook startup; startup exceeding that budget yields RPC−32603 with no result, and the isError-only assertion lets a missing payload reach JSON decoding. Normal startup before the same2-second widget budget fixes that demonstrated setup weakness. The original cold raw response was not captured, so its specific cause remains unconfirmed; all ten unchanged replays pass. | Preserve exact failed cold, run the identical original case with passive full response/state/pipe capture, diagnose demonstrated cause, retain meaningful widget settlement/reset/repeat assertions, then rerun required complete final gates. | Closed in final v17 review — The single MCP fixture completes normal 120s startup before applying its original 2s widget budget, explicitly verifies healthy startup/response envelopes and retains every original action assertion. Eight controlled oracles reproduce the setup hazard and prove real reset-pending sensitivity/recovery; complete source 830/40 and both current full installed suites pass. Ten unchanged original replays pass; the specific old uncaptured cold response remains unconfirmed. Both full installed/cold gates and relevant regressions pass; see [final integration](evidence/release-completion/final-delivery-review/V17-FINAL-INTEGRATED-REVIEW.md) and the [preserved focused ledger](evidence/release-completion/final-delivery-review/PRE-FINAL-review-log.md). |
