% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/packages.R
\name{alder_env}
\alias{alder_env}
\title{Snapshot, inspect, or restore a reproducible project environment}
\usage{
alder_env(
  action = c("status", "snapshot", "restore"),
  path = NULL,
  packages = NULL,
  clean = FALSE
)
}
\arguments{
\item{action}{One of \code{"status"}, \code{"snapshot"}, or \code{"restore"}.}

\item{path}{Notebook path or project directory. Defaults to the working
directory.}

\item{packages}{Packages to snapshot. Defaults to Alder's project package
declarations.}

\item{clean}{For restore, remove packages not recorded by the lockfile.}
}
\value{
A structured environment status list, invisibly for mutating
actions.
}
\description{
Alder uses an ordinary project-level \code{renv.lock} and the standard \code{renv}
project library. \code{snapshot} records declared packages (and their recursive
dependencies) from currently available libraries. \code{restore} installs the
exact lockfile records into the project library. \code{status} is read-only and
reports missing, mismatched, and declared-but-unlocked packages.
}
\examples{
\dontrun{
alder_env("snapshot", "analysis.R", packages = c("ggplot2", "dplyr"))
alder_env("status", "analysis.R")
alder_env("restore", "analysis.R")
}
}
